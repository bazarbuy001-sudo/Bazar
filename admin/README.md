# Админ-панель - Bazar Buy

## 📋 Обзор

Полнофункциональная админ-панель для управления проектом Bazar Buy. Разработана на vanilla JavaScript (без фреймворков) для простоты и быстрого развёртывания.

## 🚀 Структура проекта

```
/admin/
├── index.html                    # Главная страница (логин + дашборд)
├── README.md                     # Этот файл
├── css/
│   └── admin-styles.css         # Все стили админ-панели
├── js/
│   ├── api-client.js            # HTTP клиент для API
│   ├── auth.js                  # Логин, логаут, управление токенами
│   ├── dashboard.js             # Логика дашборда и навигации
│   ├── products-management.js   # Управление товарами
│   ├── orders-management.js     # Управление заказами
│   ├── clients-management.js    # Управление клиентами
│   └── messages-chat.js         # Чат менеджер-клиент
└── assets/                      # Папка для изображений и файлов (пока пуста)
```

## 🔐 Аутентификация

### Логин
1. Откройте `/admin/index.html`
2. Введите Email и пароль администратора
3. Токен сохранится в `localStorage` автоматически
4. При разрыве соединения (401) - автоматический редирект на логин

### Сессия
- Токены хранятся в `localStorage`
- Автоматическое обновление токена каждые 55 минут
- При логауте - полная очистка сессии

## 📊 Функциональность

### 1. **Дашборд** (`view-dashboard`)
- **Метрики:**
  - Всего заказов
  - Общий доход
  - Новые клиенты
  - Средний размер заказа
  
- **Статусы заказов:**
  - На обработку
  - Подтверждены
  - На отправку
  - Отправлены
  - Доставлены

- **Последние заказы:** Таблица с 5 последними заказами

### 2. **Управление товарами** (`view-products`)
- **Список товаров:**
  - Таблица со всеми товарами
  - Поиск по названию/ID
  - Фильтр по категориям
  
- **Операции:**
  - ➕ Добавить товар (форма в модальном окне)
  - ✏️ Редактировать товар
  - 🗑️ Удалить товар
  - 📥 Импорт из JSON/CSV

- **Поля товара:**
  - Название
  - Описание
  - Цена (текущая и старая)
  - Категория
  - Цвета (список)

### 3. **Управление заказами** (`view-orders`)
- **Список заказов:**
  - Таблица со всеми заказами
  - Поиск по ID/клиенту
  - Фильтр по статусу
  
- **Операции:**
  - 👁️ Просмотр деталей заказа
  - 🔄 Изменение статуса
  - 🖨️ Печать счета (PDF)

- **Статусы заказов:**
  - pending → На обработку
  - confirmed → Подтвержден
  - processing → На отправку
  - shipped → Отправлен
  - delivered → Доставлен

### 4. **Управление клиентами** (`view-clients`)
- **Список компаний:**
  - ID клиента
  - Наименование компании
  - Email
  - Телефон
  - Город
  - Статус (активен/заблокирован)

- **Операции:**
  - 👁️ Просмотр профиля компании
  - 📋 История заказов
  - 📈 Статистика
  - 🚫 Блокировка/разблокировка

### 5. **Чат** (`view-messages`)
- **Интерфейс:**
  - Список активных чатов (слева)
  - Экран переписки (справа)
  
- **Функции:**
  - Выбор чата с клиентом
  - Отправка сообщений
  - История переписки
  - Временные метки сообщений

## 🔌 API Endpoints

Все запросы идут на `/api/v1/admin/`

### Аутентификация
```
POST   /login              - Логин (email, password)
POST   /logout             - Логаут
POST   /refresh-token      - Обновление токена
```

### Товары
```
GET    /products           - Список товаров (+ фильтры)
GET    /products/:id       - Детали товара
POST   /products           - Создать товар
PUT    /products/:id       - Обновить товар
DELETE /products/:id       - Удалить товар
POST   /products/import    - Импорт из файла (JSON/CSV)
```

### Заказы
```
GET    /orders             - Список заказов (+ фильтры)
GET    /orders/:id         - Детали заказа
PUT    /orders/:id         - Обновить статус заказа
GET    /orders/stats       - Статистика заказов
```

### Клиенты
```
GET    /clients            - Список клиентов
GET    /clients/:id        - Профиль клиента
PUT    /clients/:id        - Обновить данные клиента
PUT    /clients/:id/block  - Заблокировать клиента
PUT    /clients/:id/unblock- Разблокировать клиента
GET    /clients/:id/orders - Заказы клиента
```

### Дашборд
```
GET    /dashboard          - Метрики и статистика
```

### Сообщения
```
GET    /chats              - Список чатов
GET    /messages/:id       - Сообщения чата
POST   /messages/:id       - Отправить сообщение
```

## 📝 Формат данных

### Токен JWT
```
Header: Authorization: Bearer <token>
```

### Ошибки
```json
{
  "status": 400,
  "message": "Описание ошибки",
  "details": {}
}
```

### Успешные ответы
```json
{
  "status": 200,
  "data": {},
  "message": "Success"
}
```

## ⚙️ Конфигурация

### API Base URL
В файле `js/api-client.js`:
```javascript
const apiClient = new AdminAPIClient(
  'http://localhost:3000/api/v1/admin'
);
```

При необходимости изменить URL сервера, измените `baseURL` в конструкторе.

## 🎨 Кастомизация

### Цвета и стили
В `css/admin-styles.css` есть CSS переменные:
```css
:root {
  --primary: #1976d2;
  --primary-dark: #1565c0;
  --secondary: #757575;
  --success: #4caf50;
  --danger: #f44336;
  --warning: #ff9800;
  --info: #2196f3;
}
```

### Язык
Все строки на русском. Для интернационализации отредактируйте:
- `js/dashboard.js` - названия статусов, ролей
- `index.html` - текст интерфейса
- Остальные JS файлы

## 🔒 Безопасность

1. **Аутентификация:**
   - JWT tokens в localStorage
   - Автоматический логаут при 401
   - CORS заголовки

2. **Валидация:**
   - Проверка авторизации перед доступом
   - Валидация форм на клиенте
   - Проверка ролей администратора

3. **XSS Protection:**
   - textContent вместо innerHTML где возможно
   - Экранирование данных

## 🐛 Отладка

### Логирование
Все ошибки логируются в консоль браузера (F12)

### Common Issues

**Проблема:** Черный экран после логина
- Решение: Проверьте URL в api-client.js

**Проблема:** "Unauthorized" ошибки
- Решение: Проверьте, что токен передается в headers

**Проблема:** Данные не загружаются
- Решение: Откройте Network tab в DevTools и проверьте API ответы

## 📦 Развёртывание

### На локальном хосте
```bash
# Откройте в браузере
file:///Users/bazarbuy/Desktop/fabric-store/admin/index.html

# Или используйте простой HTTP сервер
cd /Users/bazarbuy/Desktop/fabric-store
python3 -m http.server 8000
# Откройте http://localhost:8000/admin/
```

### На продакшене
1. Скопируйте папку `/admin/` на веб-сервер
2. Обновите `baseURL` в `api-client.js` на боевой URL
3. Убедитесь в настройке CORS на backend
4. Используйте HTTPS для безопасности

## 🔄 Интеграция с Backend

Backend должен иметь следующие готовые endpoints:

### Обязательные для PHASE 4
- POST `/api/v1/admin/login`
- GET `/api/v1/admin/dashboard`
- GET `/api/v1/admin/products`
- POST `/api/v1/admin/products`
- PUT `/api/v1/admin/products/:id`
- DELETE `/api/v1/admin/products/:id`
- GET `/api/v1/admin/orders`
- GET `/api/v1/admin/orders/:id`
- PUT `/api/v1/admin/orders/:id`
- GET `/api/v1/admin/clients`
- GET `/api/v1/admin/clients/:id`
- GET `/api/v1/admin/chats`
- GET `/api/v1/admin/messages/:id`
- POST `/api/v1/admin/messages/:id`

### Response Format

Все endpoints должны возвращать JSON с структурой:
```json
{
  "status": 200,
  "data": { /* данные */ },
  "message": "Success"
}
```

При ошибке:
```json
{
  "status": 400,
  "message": "Error message",
  "details": {}
}
```

## 📋 Чек-лист для завершения

- [x] Создана структура файлов
- [x] Стили админ-панели
- [x] Логин и аутентификация
- [x] Дашборд с метриками
- [x] Управление товарами
- [x] Управление заказами
- [x] Управление клиентами
- [x] Сообщения/чат
- [ ] Backend endpoints (должны быть реализованы)
- [ ] CORS настройки
- [ ] Тестирование на продакшене
- [ ] Документирование API

## 🤝 Поддержка

При возникновении вопросов или проблем:
1. Проверьте консоль браузера (F12)
2. Проверьте Network tab для API ошибок
3. Проверьте, что backend endpoints работают
4. Убедитесь в наличии CORS заголовков

## 📄 Лицензия

Part of Bazar Buy project
