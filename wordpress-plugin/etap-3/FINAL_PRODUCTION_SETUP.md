# 🚀 ФИНАЛЬНАЯ ПРОДАКШЕН-УСТАНОВКА

**Версия:** 3.0.0  
**Цель:** Полная установка всех компонентов для production использования

---

## 📋 ПРЕДВАРИТЕЛЬНЫЕ ТРЕБОВАНИЯ

### Системные требования:
- ✅ WordPress 5.8 или выше
- ✅ PHP 7.4 или выше
- ✅ MySQL 5.7 или выше
- ✅ Права на запись в папку плагина
- ✅ Права на создание таблиц БД
- ✅ Права администратора WordPress

---

## 🔧 ШАГ 1: ПОДГОТОВКА СТРУКТУРЫ ПЛАГИНА

### 1.1 Создать папку плагина

```bash
# На сервере WordPress
mkdir -p wp-content/plugins/bazarbuy-cabinet
cd wp-content/plugins/bazarbuy-cabinet
```

### 1.2 Создать структуру папок

```bash
mkdir -p includes/api
mkdir -p includes/db
mkdir -p includes/roles
mkdir -p admin-ui
mkdir -p migrations
mkdir -p tests
```

---

## 📦 ШАГ 2: КОПИРОВАНИЕ ФАЙЛОВ

### 2.1 Скопировать контроллеры

```bash
# Из локальной папки wordpress-plugin/etap-3/

# Контроллеры API
cp includes/api/class-auth-controller.php includes/api/
cp includes/api/class-user-controller.php includes/api/
cp includes/api/class-chat-controller.php includes/api/
cp includes/api/class-telegram-controller.php includes/api/
cp includes/api/class-admin-chat-controller.php includes/api/
cp includes/api/class-telegram-webhook-controller.php includes/api/

# Абстракция БД
cp includes/db/class-chat-db.php includes/db/

# Система ролей (опционально)
cp includes/roles/class-roles-manager.php includes/roles/

# Админ UI
cp admin-ui/admin-chat.js admin-ui/

# SQL миграции
cp migrations/001_create_chat_tables.sql migrations/
cp migrations/002_create_chat_threads_reads.sql migrations/
```

### 2.2 Скопировать главный файл плагина

```bash
# Скопировать главный файл
cp bazarbuy-cabinet.php .
```

**Или создать вручную:**
- Использовать содержимое из `bazarbuy-cabinet.php`

---

## ⚙️ ШАГ 3: КОНФИГУРАЦИЯ wp-config.php

Добавьте в `wp-config.php` (перед строкой `/* That's all, stop editing! */`):

```php
// ═══════════════════════════════════════════════════════════════════════════
// НАСТРОЙКИ BAZARBUY CABINET PLUGIN
// ═══════════════════════════════════════════════════════════════════════════

// JWT Secret для аутентификации (ОБЯЗАТЕЛЬНО!)
// Генерируйте уникальную случайную строку минимум 32 символа
define('BAZARBUY_JWT_SECRET', 'bazarbuy_' . bin2hex(random_bytes(32)) . '_secret_2026');

// Режимы работы
// Для разработки (mock режим):
// define('BAZARBUY_CHAT_USE_DB', false);
// define('BAZARBUY_TELEGRAM_ENABLED', false);

// Для продакшена:
define('BAZARBUY_CHAT_USE_DB', true);           // Использовать MySQL
define('BAZARBUY_TELEGRAM_ENABLED', true);      // Реальная отправка в Telegram

// Telegram конфигурация (если BAZARBUY_TELEGRAM_ENABLED = true)
define('BAZARBUY_TELEGRAM_BOT_TOKEN', 'ВАШ_BOT_TOKEN_ОТ_BOTFATHER');
define('BAZARBUY_TELEGRAM_CHAT_ID', 'ВАШ_CHAT_ID');  // Личный или групповой чат
define('BAZARBUY_TELEGRAM_SECRET_TOKEN', bin2hex(random_bytes(16)));  // Для webhook

// Настройки логирования (рекомендуется для production)
define('WP_DEBUG', false);           // false в production
define('WP_DEBUG_LOG', true);        // true для логирования ошибок
define('WP_DEBUG_DISPLAY', false);   // false в production
```

**Важно:**
- Замените `BAZARBUY_JWT_SECRET` на уникальную случайную строку
- Получите Telegram Bot Token у @BotFather
- Получите Chat ID (см. инструкцию ниже)

---

## 🗄️ ШАГ 4: SQL МИГРАЦИЯ

### 4.1 Выполнение основной миграции

```bash
# Вариант А: Через WP-CLI
wp db query < migrations/001_create_chat_tables.sql

# Вариант Б: Через phpMyAdmin
# Откройте phpMyAdmin → Выберите базу данных → SQL → Вставьте содержимое файла

# Вариант В: Через MySQL командную строку
mysql -u username -p database_name < migrations/001_create_chat_tables.sql
```

**Перед выполнением:**
- Замените `wp_` на ваш префикс таблиц (проверьте в `wp-config.php`: `$table_prefix`)

### 4.2 Проверка создания таблицы

```bash
# Через WP-CLI
wp db query "SHOW TABLES LIKE '%bazarbuy_chat_messages%'"

# Ожидается:
# wp_bazarbuy_chat_messages
```

### 4.3 Расширенная миграция (опционально)

Если нужны диалоги и прочтения:

```bash
wp db query < migrations/002_create_chat_threads_reads.sql
```

---

## 🔌 ШАГ 5: АКТИВАЦИЯ ПЛАГИНА

### 5.1 Активация через WordPress Admin

1. Войдите в WordPress Admin
2. Перейдите в **Плагины**
3. Найдите **"BazarBuy Cabinet & Chat"**
4. Нажмите **"Активировать"**

### 5.2 Активация через WP-CLI

```bash
wp plugin activate bazarbuy-cabinet
```

### 5.3 Проверка активации

После активации проверьте:

1. **Нет ошибок в админ-панели**
2. **В меню появился пункт "BazarBuy Чат"**
3. **В логах нет критических ошибок** (`wp-content/debug.log`)

---

## 📱 ШАГ 6: НАСТРОЙКА TELEGRAM (ОПЦИОНАЛЬНО)

### 6.1 Создание Telegram Bot

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните **Bot Token** (например: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 6.2 Получение Chat ID

#### Для личного чата:
1. Напишите вашему боту любое сообщение
2. Откройте в браузере:
   ```
   https://api.telegram.org/botВАШ_BOT_TOKEN/getUpdates
   ```
3. Найдите `"chat":{"id":123456789}` — это ваш Chat ID

#### Для группового чата:
1. Добавьте бота в группу
2. Отправьте сообщение в группу
3. Откройте тот же URL
4. Найдите `"chat":{"id":-1001234567890}` (отрицательное число)

### 6.3 Добавление в wp-config.php

```php
define('BAZARBUY_TELEGRAM_BOT_TOKEN', 'ваш_bot_token');
define('BAZARBUY_TELEGRAM_CHAT_ID', 'ваш_chat_id');
```

### 6.4 Установка Webhook

```bash
curl -X POST "https://api.telegram.org/botВАШ_BOT_TOKEN/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://ваш-домен.com/wp/wp-json/cabinet/v1/telegram/webhook",
    "secret_token": "ВАШ_SECRET_TOKEN",
    "max_connections": 40,
    "allowed_updates": ["message", "edited_message"]
  }'
```

**Ожидаемый ответ:**
```json
{
  "ok": true,
  "result": true,
  "description": "Webhook was set"
}
```

---

## 🧪 ШАГ 7: ТЕСТИРОВАНИЕ

### 7.1 Проверка доступности API

```bash
# Проверка корня API
curl -X GET https://ваш-домен.com/wp/wp-json/cabinet/v1/

# Ожидается: список доступных endpoints или 404 (если нет корневого endpoint)
```

### 7.2 Тест авторизации

```bash
# Получение JWT токена
curl -X POST https://ваш-домен.com/wp/wp-json/cabinet/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password"
  }'
```

**Сохраните токен:**
```bash
TOKEN="ваш_jwt_токен"
```

### 7.3 Тест отправки сообщения

```bash
curl -X POST https://ваш-домен.com/wp/wp-json/cabinet/v1/chat/send \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message": "Тестовое сообщение",
    "orderId": "123"
  }'
```

**Ожидается:** `200 OK` с `{"success": true, "data": {"id": "...", "status": "pending"}}`

### 7.4 Тест админ-чата

1. Войдите в WordPress Admin
2. Перейдите в **BazarBuy Чат**
3. Должен открыться интерфейс чата
4. Проверьте консоль браузера (F12) — не должно быть ошибок

---

## ✅ ЧЕКЛИСТ УСТАНОВКИ

### Файлы:
- [ ] Все контроллеры скопированы в `includes/api/`
- [ ] `class-chat-db.php` скопирован в `includes/db/`
- [ ] `admin-chat.js` скопирован в `admin-ui/`
- [ ] SQL миграции скопированы в `migrations/`
- [ ] Главный файл `bazarbuy-cabinet.php` создан

### Конфигурация:
- [ ] `BAZARBUY_JWT_SECRET` добавлен в `wp-config.php`
- [ ] Режимы работы настроены (`BAZARBUY_CHAT_USE_DB`, `BAZARBUY_TELEGRAM_ENABLED`)
- [ ] Telegram настройки добавлены (если используется)

### База данных:
- [ ] SQL миграция `001_create_chat_tables.sql` выполнена
- [ ] Таблица `wp_bazarbuy_chat_messages` создана
- [ ] Индексы созданы корректно

### Плагин:
- [ ] Плагин активирован в WordPress
- [ ] Нет ошибок при активации
- [ ] Пункт меню "BazarBuy Чат" появился в админке
- [ ] Страница админ-чата открывается

### Telegram (если используется):
- [ ] Bot создан через @BotFather
- [ ] Bot Token добавлен в `wp-config.php`
- [ ] Chat ID получен и добавлен
- [ ] Webhook установлен

### Тестирование:
- [ ] API endpoints доступны
- [ ] JWT токен получается успешно
- [ ] Сообщения отправляются
- [ ] История чата получается
- [ ] Админ-чат работает

---

## 🔍 ПРОВЕРКА УСТАНОВКИ

### Через WP-CLI:

```bash
# Проверить статус плагина
wp plugin list | grep bazarbuy

# Проверить таблицы БД
wp db query "SHOW TABLES LIKE '%bazarbuy%'"

# Проверить REST API
wp rest route list | grep cabinet
```

### Через WordPress Admin:

1. **Плагины** → найти "BazarBuy Cabinet & Chat" → статус должен быть "Активирован"
2. **BazarBuy Чат** → должна открыться страница чата
3. **Инструменты → Site Health** → проверить на ошибки

---

## 📊 ДОСТУПНЫЕ ENDPOINTS

После установки доступны следующие endpoints:

### Для клиентов:
- `GET /wp-json/cabinet/v1/auth/me` — проверка авторизации
- `POST /wp-json/cabinet/v1/auth/login` — вход
- `POST /wp-json/cabinet/v1/auth/register` — регистрация
- `GET /wp-json/cabinet/v1/user/client` — профиль клиента
- `GET /wp-json/cabinet/v1/chat/history` — история чата
- `POST /wp-json/cabinet/v1/chat/send` — отправка сообщения

### Для менеджеров (админ):
- `GET /wp-json/cabinet/v1/admin/chat/threads` — список диалогов
- `GET /wp-json/cabinet/v1/admin/chat/messages` — сообщения диалога
- `POST /wp-json/cabinet/v1/admin/chat/send` — отправка от менеджера
- `POST /wp-json/cabinet/v1/admin/chat/mark-read` — отметка прочитанных

### Telegram:
- `POST /wp-json/cabinet/v1/telegram/notify` — отправка уведомления
- `POST /wp-json/cabinet/v1/telegram/webhook` — обработка webhook

---

## ⚠️ РЕШЕНИЕ ПРОБЛЕМ

### Проблема: Плагин не активируется

**Решение:**
1. Проверьте логи: `wp-content/debug.log`
2. Проверьте версию PHP: `php -v` (должна быть 7.4+)
3. Проверьте версию WordPress: `wp core version` (должна быть 5.8+)
4. Проверьте, что все файлы скопированы

---

### Проблема: "BAZARBUY_JWT_SECRET не определен"

**Решение:**
1. Откройте `wp-config.php`
2. Добавьте перед `/* That's all, stop editing! */`:
   ```php
   define('BAZARBUY_JWT_SECRET', 'ваш_секрет');
   ```
3. Перезагрузите WordPress

---

### Проблема: Таблица не создаётся

**Решение:**
1. Проверьте права на создание таблиц в MySQL
2. Проверьте префикс таблиц в `wp-config.php`
3. Выполните миграцию вручную через phpMyAdmin
4. Проверьте логи: `wp-content/debug.log`

---

### Проблема: Endpoints не доступны (404)

**Решение:**
1. Выполните сброс постоянных ссылок:
   ```bash
   wp rewrite flush
   ```
2. Или в WordPress Admin: **Настройки → Постоянные ссылки → Сохранить изменения**

---

### Проблема: Админ-чат не загружается

**Решение:**
1. Проверьте консоль браузера (F12)
2. Проверьте, что файл `admin-ui/admin-chat.js` существует
3. Проверьте права доступа к файлу (644)
4. Проверьте, что пользователь имеет права `manage_options`

---

## 🎯 ФИНАЛЬНАЯ ПРОВЕРКА

После выполнения всех шагов:

✅ **Плагин активирован** без ошибок  
✅ **Все 6 контроллеров** загружены  
✅ **REST API endpoints** доступны  
✅ **Страница админ-чата** отображается  
✅ **Таблицы БД** созданы (если используется БД)  
✅ **JWT аутентификация** работает  
✅ **Rate limiting** функционирует  
✅ **Telegram** настроен (если используется)  

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

После успешной установки:

1. **Протестируйте через фронтенд:**
   - Откройте кабинет
   - Отправьте сообщение
   - Проверьте получение ответа от менеджера

2. **Настройте мониторинг:**
   - Uptime Robot для API endpoints
   - Telegram alerts для критических ошибок
   - Sentry для отслеживания ошибок

3. **Оптимизируйте производительность:**
   - Настройте кэширование
   - Оптимизируйте запросы БД
   - Настройте индексы

---

**Система готова к production использованию! 🚀**


