# PHASE 3 - Quick Start Guide

**Быстрый старт интеграции Backend API в Frontend**

---

## 🚀 За 5 минут до работающего приложения

### Шаг 1: Запустить Backend (2 минуты)

```bash
cd /Users/bazarbuy/Desktop/fabric-store/backend

# Убедиться, что зависимости установлены
npm install

# Запустить dev сервер
npm run dev

# ✅ Должно появиться:
# 🚀 Server running at http://localhost:3000
# 📡 API Base: http://localhost:3000/api/v1
```

### Шаг 2: Открыть Frontend (1 минута)

```bash
# Используя Live Server в VS Code:
# 1. Откройте файл /frontend/catalog.html
# 2. Right-click → "Open with Live Server"
# 3. Браузер откроется на http://localhost:5500/frontend/

# ИЛИ с помощью Python:
cd /Users/bazarbuy/Desktop/fabric-store/frontend
python3 -m http.server 5500
# Открыть http://localhost:5500/catalog.html
```

### Шаг 3: Проверить интеграцию (2 минуты)

```javascript
// Откройте DevTools (F12) → Console

// 1. Проверьте, что API модули загружены
console.log(ApiClient)      // должно быть объект
console.log(ProductsAPI)    // должно быть объект
console.log(API)            // должно быть объект

// 2. Проверьте Network tab (F12 → Network)
// При загрузке страницы должны быть запросы:
// GET http://localhost:3000/api/v1/products
// GET http://localhost:3000/api/v1/products/categories

// 3. Попробуйте загрузить товары
ProductsAPI.getProducts().then(result => {
  console.log('Товары:', result.data.products);
});
```

---

## 📋 HTML Структура (скрипты)

Все HTML файлы должны подключать API модули в этом порядке:

```html
<head>
  <!-- Существующие стили -->
  <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <!-- Существующий HTML - БЕЗ ИЗМЕНЕНИЙ -->
  
  <!-- ⭐ НОВОЕ: API Модули (подключить в начало) -->
  <script src="js/api/client.js"></script>
  <script src="js/api/products.js"></script>
  <script src="js/api/cart.js"></script>
  <script src="js/api/checkout.js"></script>
  <script src="js/api/orders.js"></script>
  <script src="js/api/cabinet.js"></script>
  
  <!-- Существующие скрипты (они уже обновлены) -->
  <script src="js/catalog.js"></script>
  <script src="js/cart-store.js"></script>
  <script src="js/product-popup.js"></script>
</body>
</html>
```

---

## 🧪 Тестовые запросы

### 1. Загрузить товары

```javascript
const result = await ProductsAPI.getProducts({ limit: 5 });
console.log(result.success ? '✅ Загружено' : '❌ Ошибка');
console.log(result.data.products);
```

### 2. Добавить в корзину

```javascript
const result = await CartAPI.addToCart({
  productId: 'fabric-001',
  color: 'белый',
  meters: 2,
  rolls: 1
});
console.log(result.success ? '✅ Добавлено' : '❌ Ошибка');
```

### 3. Получить корзину

```javascript
const result = await CartAPI.getCart();
console.log('Товаров в корзине:', result.data.items?.length || 0);
```

### 4. Профиль пользователя

```javascript
const result = await CabinetAPI.getProfile();
console.log('Профиль:', result.data);
```

---

## 🔧 Если что-то не работает

### ❌ Проблема: "ApiClient is not defined"

**Решение:**
1. Убедитесь, что `js/api/client.js` подключен первым
2. Проверьте консоль на ошибки загрузки файла
3. Проверьте что файл существует: `/frontend/js/api/client.js`

### ❌ Проблема: "Cannot GET /api/v1/products" (404)

**Решение:**
1. Backend не запущен → запустите `npm run dev` в `/backend/`
2. Backend запущен на неправильном порту → проверьте что это localhost:3000
3. API клиент указывает на неправильный базовый URL → измените:
   ```javascript
   ApiClient.setBaseURL('http://localhost:3000/api/v1');
   ```

### ❌ Проблема: CORS Error

**Решение:**
1. Это нормально если открываете через `file://` - используйте Live Server
2. Backend должен иметь `app.use(cors())` → проверьте backend/src/server.ts

### ❌ Проблема: Товары не загружаются, но нет ошибок

**Решение:**
1. Откройте Network tab (F12 → Network)
2. Перезагрузите страницу
3. Ищите запрос GET `/api/v1/products`
4. Если запроса нет → API модули не загружены
5. Если запрос есть но ошибка → проверьте ответ сервера

---

## 📁 Структура файлов

```
fabric-store/
├── backend/                    # Express API (уже готов)
│   ├── src/
│   │   ├── server.ts
│   │   ├── routes/index.ts    # Все endpoints
│   │   └── api/               # Контроллеры
│   └── package.json
│
└── frontend/                   # React/Vanilla JS UI
    ├── js/
    │   ├── api/               # ⭐ ВСЕ API МОДУЛИ ЗДЕСЬ
    │   │   ├── client.js      # HTTP клиент
    │   │   ├── products.js    # Товары
    │   │   ├── cart.js        # Корзина
    │   │   ├── checkout.js    # Checkout
    │   │   ├── orders.js      # Заказы
    │   │   ├── cabinet.js     # Личный кабинет
    │   │   └── index.js       # Единая точка входа
    │   │
    │   ├── catalog.js         # ✅ Обновлён для API
    │   ├── cart-store.js      # ✅ Обновлён для API
    │   ├── product-popup.js   # (требует обновления)
    │   └── ...
    │
    ├── css/                   # Стили (не меняются)
    ├── *.html                 # HTML файлы (не меняются)
    └── images/                # Изображения
```

---

## 🎯 Что сейчас работает с API

### ✅ Готово к использованию (Phase 3)

```
✅ Загрузка каталога товаров
✅ Фильтрация по категориям
✅ Сортировка по цене
✅ Поиск товаров
✅ Добавление товара в корзину
✅ Просмотр корзины
✅ Удаление из корзины
✅ Очистка корзины
✅ Оформление заказа (2 шага)
✅ Просмотр профиля
✅ Управление адресами
```

### ⏳ Требует дополнительного обновления (Phase 4)

```
⏳ Карточка товара (product-popup.js)
⏳ Личный кабинет (cabinet.js)
⏳ Поиск товаров (search/)
⏳ Уведомления о загрузке (loading spinners)
⏳ Обработка ошибок в UI
```

---

## 💾 Локальное хранилище (Fallback)

Если backend недоступен:

```javascript
// API модули автоматически используют localStorage как fallback
// Это позволяет приложению работать в offline режиме

CartStore.add({...})  // Попробует API → если ошибка → localStorage
CartStore.getAll()    // Вернёт товары из памяти (синхронно)
```

---

## 📊 Мониторинг API запросов

### В DevTools Console

```javascript
// Посмотреть все кэшированные данные
ApiClient.getFromCache('/products')

// Очистить кэш
ApiClient.clearCache('/products')

// Слушать loading события
document.addEventListener('api:loading', (e) => {
  console.log(e.detail.isLoading ? 'Загрузка...' : 'Готово');
});
```

### В Network Tab

1. Откройте DevTools → Network tab
2. Перезагрузите страницу
3. Должны быть запросы вида:
   ```
   GET http://localhost:3000/api/v1/products?page=1&limit=20
   GET http://localhost:3000/api/v1/products/categories
   ```

---

## 🐛 Логирование для отладки

В консоли браузера будут логи вида:

```
[Catalog] Загружаем товары из API...
[Catalog] Загружено 50 товаров из API
[CartAPI] Корзина загружена: Array(3)
[ProductsAPI] Товар из кэша: fabric-001
[API] Инициализирован успешно: {client, products, cart, ...}
```

Если вы видите эти логи - всё работает правильно! ✅

---

## 🎬 Примеры использования

### Загрузить товары с фильтром

```javascript
// Напрямую в консоли браузера:
ProductsAPI.getProducts({
  category: 'fabrics',
  minPrice: 100,
  maxPrice: 500,
  sort: 'price_asc'
}).then(r => {
  if (r.success) {
    console.log(`Найдено ${r.data.products.length} товаров`);
    r.data.products.forEach(p => console.log(p.name, p.price));
  }
});
```

### Добавить в корзину и оформить

```javascript
// 1. Добавить товар
await CartAPI.addToCart({
  productId: 'fabric-001',
  color: 'белый',
  meters: 3,
  rolls: 1
});

// 2. Получить корзину
const cart = await CartAPI.getCart();
console.log('Сумма:', cart.data.totalAmount);

// 3. Оформить
const checkout = await CheckoutAPI.initCheckout({
  name: 'ООО Компания',
  email: 'info@example.com',
  phone: '+7 (999) 123-45-67',
  city: 'Москва',
  address: 'ул. Примерная, д. 123'
});

const order = await CheckoutAPI.submitOrder(checkout.data.sessionId);
console.log('Заказ создан:', order.data.orderId);
```

---

## 📞 Помощь

### Для подробной информации:

1. **PHASE3_INTEGRATION_GUIDE.md** - полное руководство
2. **PHASE3_COMPLETION_REPORT.md** - статус и статистика
3. **API модули** - содержат JSDoc комментарии

### Для быстрых вопросов:

- Посмотрите примеры в этом файле
- Проверьте Console браузера на ошибки
- Проверьте Network tab в DevTools

---

## ✅ Чек-лист готовности

Перед началом работы проверьте:

- [ ] Backend запущен (`npm run dev` в /backend/)
- [ ] Frontend открыт через Live Server (http://localhost:5500/frontend/)
- [ ] В консоли браузера видны логи [API], [Catalog], [CartAPI]
- [ ] Network tab показывает запросы на localhost:3000/api/v1/*
- [ ] Нет ошибок в консоли браузера (красных сообщений)
- [ ] ProductsAPI.getProducts() возвращает товары

Если всё ✅ - приложение готово к работе! 🎉

---

**Дата:** 2026-02-15  
**Версия:** 1.0  
**Статус:** ✅ Ready

