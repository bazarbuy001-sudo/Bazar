# Диагностический отчет: Прыжок шапки при загрузке страницы

## Дата анализа
Полный диагностический анализ причин горизонтального и вертикального смещения шапки при перезагрузке страницы.

---

## 1. Структура DOM шапки

### Полное дерево элементов (только классы):

```
.site-header
  └── .nav-container
      └── .header-row-1 (#headerRow1)
          ├── .row1-brand ("Bazar Buy")
          └── .row1-nav
              ├── .nav-link (5 ссылок: Оптовые скидки, Оплата, Доставка, FAQ, Контакты)
              └── ...

.header-sticky (#headerSticky)
  └── .nav-container
      └── .header-row-2
          ├── .logo-icon
          │   └── img[src="icons/LogoBazarbuy.svg"]
          ├── .row2-brand ("Bazar Buy")
          ├── .catalog-btn
          ├── .search-box
          ├── .row2-contacts
          └── ...
```

---

## 2. Анализ CSS

### 2.1 Начальные стили (до выполнения JavaScript)

#### `.header-sticky`
```css
background: white;
border-bottom: 1px solid var(--border-color);
box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
transition: box-shadow 0.3s ease;
width: 100%;
overflow: visible;
box-sizing: border-box;
```

#### `.header-row-1`
```css
display: flex;
align-items: center;
justify-content: flex-start;
position: relative;
padding: 8px 20px 8px 20px;
min-height: 50px;
max-height: 100px;
width: 100%;
overflow: visible;
box-sizing: border-box;
transition: max-height 0.3s ease, opacity 0.3s ease, padding 0.3s ease, min-height 0.3s ease, height 0.3s ease;
```

#### `.header-row-2`
```css
display: flex;
align-items: flex-end;
justify-content: center;
padding: 0px 20px 10px 20px;
gap: 12px;
width: 100%;
box-sizing: border-box;
overflow: visible;
margin-top: -30px;
opacity: 0; /* ⚠️ СКРЫТ НАЧАЛЬНО */
transition: padding 0.3s ease, gap 0.3s ease, margin-top 0.3s ease;
```

#### `.nav-container`
```css
max-width: 60%;
width: 60%;
margin: 0 auto;
position: relative;
overflow: visible;
```

#### `.logo-icon`
```css
width: 72px;
height: 72px;
background: transparent;
border-radius: 10px;
display: flex;
align-items: center;
justify-content: center;
flex-shrink: 0;
padding: 0;
overflow: visible;
box-sizing: border-box;
margin-top: 5px;
transform: translateX(20px);
transition: width 0.3s ease, height 0.3s ease, margin-top 0.3s ease, transform 0.3s ease;
position: relative;
z-index: 15;
```

#### `.row1-brand`
```css
font-family: 'Didot', serif;
font-weight: 700;
font-size: 24px;
position: absolute;
left: var(--brand-center-left, 50%); /* ⚠️ ПО УМОЛЧАНИЮ 50% */
top: 0;
margin-top: 10px;
opacity: 0; /* ⚠️ СКРЫТ НАЧАЛЬНО */
visibility: hidden; /* ⚠️ СКРЫТ НАЧАЛЬНО */
pointer-events: none;
transition: opacity 0.3s ease, visibility 0.3s ease, margin-top 0.3s ease;
z-index: 5;
```

#### `.row1-nav`
```css
display: flex;
align-items: center;
gap: 24px;
position: absolute;
left: var(--nav-center-left, 50%); /* ⚠️ ПО УМОЛЧАНИЮ 50% */
top: 50%;
transform: translate(-50%, -50%);
z-index: 10;
pointer-events: auto;
```

### 2.2 Финальные стили (после выполнения JavaScript)

#### `.header-row-2.ready`
```css
opacity: 1; /* ✅ ПОКАЗЫВАЕТСЯ ПОСЛЕ JS */
transition: padding 0.3s ease, gap 0.3s ease, opacity 0.3s ease, margin-top 0.3s ease;
```

#### `.row1-brand.positioned`
```css
opacity: 1; /* ✅ ПОКАЗЫВАЕТСЯ ПОСЛЕ JS */
visibility: visible; /* ✅ ПОКАЗЫВАЕТСЯ ПОСЛЕ JS */
pointer-events: auto;
```

#### CSS переменные, установленные JavaScript:
- `--brand-center-left`: меняется с `50%` (по умолчанию) на `XXXpx` (вычисленное значение)
- `--nav-center-left`: меняется с `50%` (по умолчанию) на `XXXpx` (вычисленное значение)
- `--line-start`: устанавливается в `XXXpx`
- `--line-width`: устанавливается в `XXXpx`

### 2.3 Свойства, которые меняются между начальной и финальной отрисовкой

| Элемент | Начальное значение | Финальное значение | Влияние на layout |
|---------|-------------------|-------------------|-------------------|
| `.row1-brand` `left` | `50%` (CSS переменная по умолчанию) | `XXXpx` (устанавливается JS) | ✅ **Горизонтальный сдвиг** |
| `.row1-brand` `opacity` | `0` | `1` | Визуальный flash, но не layout shift |
| `.row1-brand` `visibility` | `hidden` | `visible` | Визуальный flash, но не layout shift |
| `.row1-nav` `left` | `50%` (CSS переменная по умолчанию) | `XXXpx` (устанавливается JS) | ✅ **Горизонтальный сдвиг** |
| `.header-row-2` `opacity` | `0` | `1` | Визуальный flash, но не layout shift |
| `.header-row-1::after` `opacity` | `0` | `1` (после `.line-ready`) | Не влияет на layout |
| `.header-row-1::after` `width` | `0px` | `XXXpx` | Не влияет на layout (pseudo-element) |

---

## 3. JavaScript анализ

### 3.1 Код, выполняемый при загрузке страницы

#### Функция `initHeader()` (строки 1723-1817)
```javascript
(function() {
    let isInitialized = false;
    
    function initHeader() {
        if (isInitialized) return;
        
        // Получает элементы DOM
        const headerSticky = document.getElementById('headerSticky');
        const headerRow1 = document.getElementById('headerRow1');
        const headerRow2 = document.querySelector('.header-row-2');
        const catalogBtn = document.querySelector('.catalog-btn');
        const row1Brand = document.querySelector('.row1-brand');
        
        // Функция центрирования
        function centerBrandOnCatalogBtn() {
            // Вычисляет позиции
            const catalogBtnLeft = catalogBtnRect.left - headerRow1Rect.left;
            
            // ⚠️ ИЗМЕНЯЕТ CSS ПЕРЕМЕННЫЕ
            headerRow1.style.setProperty('--brand-center-left', catalogBtnLeft + 'px');
            headerRow1.style.setProperty('--nav-center-left', navCenterPosition + 'px');
            headerRow1.style.setProperty('--line-start', lineStart + 'px');
            headerRow1.style.setProperty('--line-width', lineWidth + 'px');
            
            // ⚠️ ДОБАВЛЯЕТ КЛАССЫ (показывает элементы)
            headerRow1.classList.add('line-ready');
            row1Brand.classList.add('positioned');
            headerRow2.classList.add('ready');
        }
        
        // ⚠️ МНОЖЕСТВЕННЫЕ ПОПЫТКИ С ЗАДЕРЖКАМИ
        const attempts = [0, 50, 100, 200, 300];
        attempts.forEach(delay => {
            setTimeout(() => {
                if (centerBrandOnCatalogBtn()) {
                    isInitialized = true;
                }
            }, delay);
        });
    }
    
    // Инициализация
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initHeader);
    } else {
        initHeader();
    }
    
    // Дополнительная попытка после загрузки
    window.addEventListener('load', () => {
        setTimeout(initHeader, 0);
    });
})();
```

#### Функция `handleScroll()` (строки 1789-1797)
```javascript
function handleScroll() {
    if (window.scrollY > 60) {
        headerSticky.classList.add('scrolled');
        headerRow1.classList.add('collapsed');
    } else {
        headerSticky.classList.remove('scrolled');
        headerRow1.classList.remove('collapsed');
    }
}

window.addEventListener('scroll', handleScroll, { passive: true });
handleScroll(); // Вызывается сразу для начального состояния
```

### 3.2 Операции, влияющие на шапку при загрузке

| Операция | Когда выполняется | Влияние на layout |
|----------|------------------|-------------------|
| Установка `--brand-center-left` | 0ms, 50ms, 100ms, 200ms, 300ms (setTimeout) | ✅ **Горизонтальный сдвиг `.row1-brand`** |
| Установка `--nav-center-left` | 0ms, 50ms, 100ms, 200ms, 300ms (setTimeout) | ✅ **Горизонтальный сдвиг `.row1-nav`** |
| Добавление `.positioned` к `.row1-brand` | После вычисления позиций | Визуальное появление (opacity: 0 → 1) |
| Добавление `.ready` к `.header-row-2` | После вычисления позиций | Визуальное появление (opacity: 0 → 1) |
| Добавление `.line-ready` к `.header-row-1` | После вычисления позиций | Визуальное появление линии |
| Проверка `handleScroll()` | Сразу при инициализации | Может добавить `.scrolled` или `.collapsed` |

### 3.3 Проблемы в логике инициализации

1. **Множественные попытки с задержками** (0ms, 50ms, 100ms, 200ms, 300ms)
   - Элементы могут появляться в разное время
   - Вызывает визуальный "мерцание"

2. **CSS переменные меняются асинхронно**
   - Начальное значение: `50%`
   - Финальное значение: `XXXpx`
   - Вызывает видимый горизонтальный сдвиг

3. **Элементы скрыты изначально** (`opacity: 0`, `visibility: hidden`)
   - Затем показываются через классы
   - Вызывает flash при появлении

---

## 4. Анализ шрифтов

### 4.1 Используемые шрифты в шапке

1. **Google Fonts:**
   - `Inter` (для общего текста)
   - `DM Sans` (основной шрифт body, кнопки)
   - Загрузка: `<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&display=swap">`
   - `font-display: swap` (по умолчанию для Google Fonts)

2. **Didot (для "Bazar Buy"):**
   - Загрузка: `@import url('https://fonts.cdnfonts.com/css/didot');`
   - Используется в: `.row1-brand`, `.row2-brand`
   - `font-display`: не указан явно (может быть `auto` или `swap`)

### 4.2 Влияние загрузки шрифтов на layout

| Элемент | Шрифт | Проблема |
|---------|-------|----------|
| `.row1-brand` | 'Didot', serif | ⚠️ Может вызвать layout shift при загрузке шрифта |
| `.catalog-btn` | 'DM Sans', sans-serif | ⚠️ Может вызвать layout shift при загрузке шрифта |
| `.nav-link` | 'DM Sans', sans-serif (наследуется) | ⚠️ Может вызвать layout shift при загрузке шрифта |

**Потенциальная проблема:**
- Шрифты загружаются асинхронно
- `font-display: swap` показывает fallback шрифт, затем заменяет его
- Это может вызвать изменение ширины текста (FOUT - Flash of Unstyled Text)

---

## 5. Анализ сети/layout

### 5.1 Изображения и SVG

| Ресурс | Тип | Размер контейнера | Влияние на layout |
|--------|-----|------------------|-------------------|
| `icons/LogoBazarbuy.svg` | SVG | Фиксированный: `72px × 72px` (нормальное), `40px × 40px` (scrolled) | ✅ Не вызывает layout shift (размер фиксирован) |

**Вывод:** Логотип имеет фиксированные размеры, поэтому его загрузка не должна вызывать layout shift.

### 5.2 Появление/исчезновение scrollbar

**Потенциальная проблема:**
- Если контент страницы загружается постепенно, scrollbar может появиться/исчезнуть
- Это меняет ширину viewport на `~15px`
- `.nav-container` имеет `width: 60%` от viewport
- При изменении ширины viewport, контейнер также меняет размер
- Это может вызвать горизонтальный сдвиг элементов внутри шапки

**Проверка:**
- `.nav-container`: `width: 60%` (процентная ширина зависит от viewport)
- `.header-row-1`: `width: 100%` (относительно `.nav-container`)
- `.header-row-2`: `width: 100%` (относительно `.nav-container`)

**Вывод:** Появление scrollbar может вызвать небольшой горизонтальный сдвиг, но это не основная причина прыжка.

---

## 6. Идентификация точной причины

### 6.1 Основные причины layout shift (CLS)

#### 🔴 **ПРИЧИНА #1: Изменение CSS переменных для позиционирования (КРИТИЧНО)**

**Что происходит:**
1. `.row1-brand` имеет начальное `left: var(--brand-center-left, 50%)`
2. По умолчанию CSS переменная не установлена, используется `50%`
3. JavaScript вычисляет реальную позицию кнопки "Каталог товаров" и устанавливает `--brand-center-left: XXXpx`
4. Элемент сдвигается с `left: 50%` на `left: XXXpx`

**Элементы, которые двигаются:**
- `.row1-brand` - горизонтальный сдвиг
- `.row1-nav` - горизонтальный сдвиг (аналогично)

**Время сдвига:** 0ms, 50ms, 100ms, 200ms, или 300ms (зависит от того, какая попытка успешна)

**Оценка влияния:** 🔴 **ВЫСОКАЯ** - видимый горизонтальный прыжок

---

#### 🟡 **ПРИЧИНА #2: Загрузка шрифтов с font-display: swap (СРЕДНЕЕ)**

**Что происходит:**
1. Шрифт `Didot` для "Bazar Buy" загружается асинхронно
2. Пока шрифт не загружен, используется fallback (serif)
3. После загрузки шрифт заменяется на `Didot`
4. Ширина текста может измениться, если шрифты имеют разную ширину символов

**Элементы, которые могут двигаться:**
- `.row1-brand` - если ширина текста меняется
- `.catalog-btn` - если ширина текста меняется
- `.nav-link` элементы - если ширина текста меняется

**Время сдвига:** Зависит от скорости загрузки шрифта (обычно 100-500ms)

**Оценка влияния:** 🟡 **СРЕДНЯЯ** - зависит от разницы в ширине символов между fallback и основным шрифтом

---

#### 🟡 **ПРИЧИНА #3: Множественные setTimeout с разными задержками (СРЕДНЕЕ)**

**Что происходит:**
1. JavaScript пытается вычислить позиции 5 раз: 0ms, 50ms, 100ms, 200ms, 300ms
2. Каждая попытка может установить CSS переменные
3. Если первая попытка неудачна (элементы еще не отрисованы), переменные устанавливаются позже
4. Это создает визуальное "мерцание" или множественные сдвиги

**Элементы, которые могут двигаться:**
- Все элементы, использующие CSS переменные (`.row1-brand`, `.row1-nav`, `.header-row-1::after`)

**Время сдвига:** 0ms, 50ms, 100ms, 200ms, 300ms (множественные сдвиги)

**Оценка влияния:** 🟡 **СРЕДНЯЯ** - создает ощущение нестабильности, но основная проблема - в изменении позиций (Причина #1)

---

#### 🟢 **ПРИЧИНА #4: Изменение opacity/visibility (НИЗКОЕ)**

**Что происходит:**
1. `.row1-brand` начинается с `opacity: 0` и `visibility: hidden`
2. После вычисления позиций добавляется класс `.positioned`
3. Элемент становится видимым (`opacity: 1`, `visibility: visible`)

**Элементы, которые меняются:**
- `.row1-brand` - визуальное появление (не layout shift, но flash)
- `.header-row-2` - визуальное появление (не layout shift, но flash)

**Оценка влияния:** 🟢 **НИЗКАЯ** - это не layout shift, но создает визуальный flash (FOUC)

---

### 6.2 Итоговая оценка причин

| Причина | Приоритет | Влияние на layout | Визуальное влияние |
|---------|-----------|------------------|-------------------|
| #1: Изменение CSS переменных | 🔴 ВЫСОКИЙ | ✅ Да (горизонтальный сдвиг) | Видимый прыжок |
| #2: Загрузка шрифтов | 🟡 СРЕДНИЙ | ⚠️ Возможно (если ширина меняется) | Возможный сдвиг текста |
| #3: Множественные setTimeout | 🟡 СРЕДНИЙ | ✅ Да (множественные сдвиги) | Ощущение нестабильности |
| #4: Изменение opacity | 🟢 НИЗКИЙ | ❌ Нет | Flash при появлении |

---

## 7. Затронутые CSS/JS файлы

### 7.1 CSS (встроенный в `index.html`)

**Строки 122-168:**
- `.row1-brand` - начальные стили с `left: var(--brand-center-left, 50%)`
- `.row1-brand.positioned` - финальные стили
- `.row1-nav` - начальные стили с `left: var(--nav-center-left, 50%)`

**Строки 203-227:**
- `.header-row-2` - начальные стили с `opacity: 0`
- `.header-row-2.ready` - финальные стили с `opacity: 1`

### 7.2 JavaScript (встроенный в `index.html`)

**Строки 1719-1830:**
- `initHeader()` - функция инициализации
- `centerBrandOnCatalogBtn()` - функция вычисления позиций
- `handleScroll()` - функция обработки скролла
- Множественные `setTimeout` вызовы

---

## 8. Безопасные варианты исправления

### 8.1 Решение #1: Предустановить CSS переменные в CSS (РЕКОМЕНДУЕТСЯ)

**Суть:**
- Вычислить приблизительные значения `--brand-center-left` и `--nav-center-left` заранее
- Установить их в CSS как начальные значения
- JavaScript будет только уточнять позиции, а не устанавливать с нуля

**Преимущества:**
- Минимальный layout shift
- Элементы появляются сразу в правильном месте (примерно)
- JavaScript только уточняет позиции

**Недостатки:**
- Требует знания примерной ширины элементов
- Может не работать идеально на всех размерах экрана

**Реализация:**
```css
.header-row-1 {
    /* Предустановленные значения (примерно) */
    --brand-center-left: 200px; /* Примерное значение */
    --nav-center-left: 400px; /* Примерное значение */
}
```

---

### 8.2 Решение #2: Использовать `content-visibility: auto` и `will-change` (АЛЬТЕРНАТИВА)

**Суть:**
- Использовать `content-visibility: auto` для оптимизации отрисовки
- Использовать `will-change: left` для `.row1-brand` и `.row1-nav`
- Это помогает браузеру оптимизировать анимации

**Преимущества:**
- Улучшает производительность
- Может уменьшить визуальный jump

**Недостатки:**
- Не решает основную проблему (изменение CSS переменных)
- Может создать новые проблемы с accessibility

---

### 8.3 Решение #3: Использовать `ResizeObserver` и `IntersectionObserver` (АЛЬТЕРНАТИВА)

**Суть:**
- Использовать `ResizeObserver` для отслеживания изменения размеров
- Использовать `IntersectionObserver` для определения, когда элементы видны
- Вычислять позиции только когда элементы полностью отрисованы

**Преимущества:**
- Более точное определение момента, когда элементы готовы
- Меньше попыток вычисления

**Недостатки:**
- Более сложная реализация
- Все еще не решает проблему начального значения `50%`

---

### 8.4 Решение #4: Скрыть элементы до готовности (ВРЕМЕННОЕ РЕШЕНИЕ)

**Суть:**
- Оставить элементы скрытыми (`opacity: 0`, `visibility: hidden`) до тех пор, пока JavaScript не вычислит позиции
- Показывать элементы только после установки CSS переменных

**Преимущества:**
- Устраняет видимый прыжок (элементы появляются сразу в правильном месте)
- Простая реализация

**Недостатки:**
- Создает задержку перед появлением элементов (FOUC)
- Плохой UX (пользователь видит пустое место)

---

### 8.5 Решение #5: Использовать статическую разметку вместо абсолютного позиционирования (РАДИКАЛЬНОЕ)

**Суть:**
- Переделать шапку, чтобы не использовать абсолютное позиционирование для `.row1-brand` и `.row1-nav`
- Использовать Flexbox или Grid для позиционирования
- Убрать зависимость от JavaScript для позиционирования

**Преимущества:**
- Полностью устраняет layout shift
- Элементы позиционируются CSS, а не JavaScript
- Лучшая производительность

**Недостатки:**
- Требует значительного рефакторинга
- Может потребовать изменения дизайна
- Трудоемкая реализация

---

## 9. Рекомендации

### 9.1 Приоритет исправлений

1. **Высокий приоритет:** Решение #1 (предустановить CSS переменные) + Решение #3 (использовать ResizeObserver)
2. **Средний приоритет:** Решение #2 (оптимизация с `will-change`)
3. **Низкий приоритет:** Решение #4 (временное скрытие)

### 9.2 Комбинированное решение (РЕКОМЕНДУЕТСЯ)

**Комбинация Решения #1 + Решения #3:**

1. Предустановить CSS переменные в CSS с примерными значениями
2. Использовать `ResizeObserver` для точного вычисления позиций
3. Уменьшить количество `setTimeout` попыток (использовать только одну попытку после `DOMContentLoaded`)
4. Убрать множественные задержки (0ms, 50ms, 100ms, 200ms, 300ms)

**Ожидаемый результат:**
- Минимальный layout shift (элементы появляются сразу в правильном месте)
- Точное позиционирование после загрузки
- Стабильная работа на всех размерах экрана

---

## 10. Заключение

### Основная причина прыжка шапки:

🔴 **Изменение CSS переменных `--brand-center-left` и `--nav-center-left` с начального значения `50%` (по умолчанию) на вычисленные значения в пикселях (устанавливаются JavaScript).**

### Дополнительные факторы:

- Множественные `setTimeout` вызовы создают ощущение нестабильности
- Загрузка шрифтов может вызвать дополнительный layout shift
- Элементы скрыты изначально, что создает визуальный flash

### Рекомендуемое решение:

Комбинация предустановки CSS переменных + использование `ResizeObserver` для точного вычисления позиций + уменьшение количества попыток инициализации.

