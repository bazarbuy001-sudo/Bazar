# ТЕХНИЧЕСКИЙ АНАЛИЗ КАТАЛОГА ТОВАРОВ BAZARBUY.STORE

**Дата:** 2025-01-10  
**Цель:** Полное описание текущей реализации каталога для подготовки ТЗ

---

## 1️⃣ КНОПКА «КАТАЛОГ ТОВАРОВ» (HEADER)

### 1.1 Где реализована
- **Файл:** `frontend/index.html`
- **Строка:** 938-941
- **HTML структура:**
  ```html
  <button class="catalog-btn">
      <span>☰</span>
      <span>Каталог товаров</span>
  </button>
  ```

### 1.2 Что она делает сейчас
- **Простая кнопка (не ссылка)**
- **Отсутствует обработчик клика** в `index.html`
- **Отсутствует JavaScript-логика** для открытия меню/каталога
- **Нет dropdown / mega menu** — кнопка не функциональна
- **Нет автo-раскрытия** подкатегорий

### 1.3 Есть ли логика
- **Hover (наведение):** есть CSS стили (`.catalog-btn:hover`, строка 340 в `index.html`)
- **Click (клик):** обработчик отсутствует
- **Автo-раскрытие:** отсутствует

### 1.4 Можно ли ТЕХНИЧЕСКИ динамически строить навигацию
- **ДА** — можно, но **ТРЕБУЕТСЯ ПЕРЕПИСЫВАНИЕ хедера**
- Причина: кнопка не имеет обработчиков и связанной логики
- Технически возможно добавить JavaScript-модуль для построения меню из данных каталога

---

## 2️⃣ КАТАЛОГ-НАВИГАЦИЯ (ВЫПАДАЮЩЕЕ МЕНЮ)

### 2.1 Есть ли сейчас
- **ОТСУТСТВУЕТ** выпадающее меню каталога
- **ОТСУТСТВУЕТ** многоуровневая навигация
- **ОТСУТСТВУЕТ** структура категорий → подкатегорий → типы ткани

### 2.2 Где хранится структура
- **ОТСУТСТВУЕТ** отдельная структура навигации
- Данные о категориях есть только в поле `category` товаров в JSON
- Поле `fabric_type` (slug) используется для фильтрации, но не для навигации

### 2.3 Можно ли сделать навигацию динамической
- **ДА** — технически возможно построить из данных `products.json`
- Источники данных: поля `category`, `fabric_type` товаров
- Автo-раскрытие по hover требует реализации с нуля

---

## 3️⃣ СТРАНИЦА КАТАЛОГА ТКАНЕЙ

### 3.1 Где реализована
- **HTML файл:** `frontend/catalog.html`
- **Основной JS модуль:** `frontend/js/catalog.js`
- **Ядро каталога:** `frontend/js/catalog/catalog-core.js`
- **Интеграция:** `frontend/js/catalog/integration.js`

### 3.2 Фильтры УЖЕ СУЩЕСТВУЮТ (все)

**В HTML (`catalog.html`):**
1. **Цвет** (single select) — строки 968-974
   - Контейнер: `#filterColors`
   - Заполняется динамически

2. **Тип ткани** (single select) — строки 976-992
   - Контейнер: `#filterFabricType`
   - **ЗАХАРДКОЖЕНЫ** кнопки: "Все", "Велюр", "Вискоза", "Габардин", "Лён", "Органза", "Поплин", "Синтетика", "Хлопок", "Шерсть", "Шёлк"
   - Значения: `data-fabric-type` атрибуты

3. **Состав** (multi select) — строки 994-1023
   - Контейнер: `#filterComposition`
   - **ЗАХАРДКОЖЕНЫ** чекбоксы: "Хлопок", "Полиэстер", "Лён", "Вискоза", "Шерсть", "Смесовые"

4. **Плотность** (range) — строки 1025-1039
   - Контейнер: `#filterDensity`
   - Диапазон: 0-1000 г/м²
   - Элементы: `#densityMin`, `#densityMax`, слайдеры `#densitySliderMin`, `#densitySliderMax`

5. **Цена** (range) — строки 1041-1055
   - Контейнер: `#filterPrice`
   - Элементы: `#priceMin`, `#priceMax`, слайдеры `#priceSliderMin`, `#priceSliderMax`
   - Диапазон слайдера: 0-10000 ₽

6. **Статус товара** (multi select) — строки 1057-1074
   - Контейнер: `#filterStatus`
   - **ЗАХАРДКОЖЕНЫ** чекбоксы: "В продаже" (`in_stock`), "В пути" (`in_transit`), "Предзаказ" (`preorder`)

**В JS модуле фильтров (`filters.js`):**
- Поддерживаются фильтры из JSON:
  - `colors` (массив строк или объектов)
  - `fabric_type` (slug, с fallback на `category`)
  - `price` (range, исключает `price_on_request=true` при активном фильтре)
  - `fabric_meterage` (range, опционально)
  - Любые другие поля, определяемые автоматически через `detectParamType()`

### 3.3 Фильтры: захардкожены или динамические

**ЗАХАРДКОЖЕНЫ в HTML:**
- Тип ткани (кнопки в `catalog.html`)
- Состав (чекбоксы в `catalog.html`)
- Статус товара (чекбоксы в `catalog.html`)

**ДИНАМИЧЕСКИЕ (заполняются из данных):**
- Цвета (строятся из `products.json` через `initColorFilter()`)
- Плотность (диапазон вычисляется из данных)
- Цена (диапазон вычисляется из данных)

### 3.4 Можно ли добавлять новые фильтры без переписывания логики

**ДА** — модуль фильтров поддерживает автоматическое определение типов:
- `CatalogDataStore.detectParamType(paramKey)` — определяет тип (boolean/range/list)
- `CatalogDataStore.getFilterableKeys()` — получает все фильтруемые ключи
- `CatalogFilters` строит фильтры автоматически из структуры данных
- **НО:** HTML нужно дополнять вручную для новых фильтров (если требуется UI)

**Риск поломки:** минимальный, если структура данных остается совместимой

---

## 4️⃣ КАРТОЧКА ТОВАРА (POPUP)

### 4.1 Где реализована
- **JS модуль:** `frontend/js/product-popup.js` (1703 строки)
- **CSS:** `frontend/css/product-popup.css` (1035 строк)

### 4.2 Что В НЕЙ УЖЕ ЕСТЬ (точный список)

**Обязательные элементы:**
1. **Название товара** — `state.product.name`
2. **Артикул** — `product.article` или `product.sku` или автогенерация `FAB-XXX`
3. **Фото:**
   - Основное изображение (`product.image`)
   - Галерея (`product.gallery[]`)
   - Изображения по цветам (`color.images[]`)
   - Миниатюры (всегда показывается 4 шт)
4. **Цвета:**
   - Поддержка строк: `colors: ["белый", "бежевый"]`
   - Поддержка объектов: `colors: [{name: "Бежевый", hex: "#e8d7c3", images: []}]`
   - Кнопка раскрытия "+N цветов" (если цветов > 8)
   - Выбор цвета меняет изображения
5. **Количество:**
   - Метры (input number, кнопки +/−)
   - Рулоны (input number, кнопки +/−)
   - Синхронизация метров ↔ рулонов
   - Минимальные значения: `product.order.min_meters` или дефолт 1
   - Метров в рулоне: `product.order.meters_per_roll` или дефолт 100
6. **Цена:**
   - Отображение с валютой (`product.price`, `product.price_unit`)
   - Конвертация валют (RUB, USD, KZT, KGS)
   - Dropdown выбора валюты
7. **Кнопка "Добавить в корзину"** — отправляет событие `product:addToCart`
8. **Кнопка закрытия** (×)

**Опциональные элементы (если есть в данных):**
9. **Описание** — `product.description`
10. **Характеристики** — `product.specs` (объект)
    - Отображаются автоматически, если есть
11. **Бейджи:**
    - "Новинка" (`is_new: true`)
    - "Акция" (`is_sale: true`)
    - "Скоро" (`coming_soon: true`)
12. **Состояние корзины:**
    - Сообщение "Уже в корзине: X м, Y рул., цвет: Z"
    - Проверка через `CartStore.getAll()`

**НЕТ в карточке:**
- Кнопка перехода в каталог
- Кнопка "посмотреть всё"
- Ссылки на похожие товары

### 4.3 Как открывается карточка
- **По клику из каталога** — через событие `catalog:openProduct`
- **Событие:** `CustomEvent('catalog:openProduct', {detail: {productId}})`
- **Отправители:**
  - `catalog.js` (строка 439)
  - `productCard.js` (строки 54, 73)
- **Получатель:** `product-popup.js` (строка 1665) — слушает событие через `document.addEventListener`

### 4.4 Есть ли в карточке
- **Кнопка перехода в каталог:** НЕТ
- **Кнопка «посмотреть всё»:** НЕТ

---

## 5️⃣ ПОИСК ПО САЙТУ

### 5.1 Где реализован поиск
- **HTML:** `frontend/catalog.html`, строка 940
  ```html
  <input type="text" class="search-input" placeholder="Поиск товаров...">
  ```
- **CSS:** стили есть (`.search-input`, `.search-box`)
- **JavaScript обработчик:** **ОТСУТСТВУЕТ** в `catalog.html`

### 5.2 По каким данным он сейчас ищет
- **Поиск НЕ РАБОТАЕТ** — отсутствует JavaScript-логика
- HTML-инпут есть, но нет обработчиков `input`, `change`, `keyup`

### 5.3 Можно ли синхронизировать поиск с каталогом
- **ДА** — технически возможно
- Данные доступны через `CatalogDataStore.getAllProducts()`
- Можно использовать `CatalogFilters.applyFilters()` для фильтрации по названию
- **ТРЕБУЕТСЯ:** добавить JavaScript-логику поиска

---

## 6️⃣ ИСТОЧНИК ДАННЫХ ТОВАРОВ

### 6.1 Откуда берутся товары
- **Основной источник:** `frontend/data/products.json`
- **Формат:** JSON массив объектов
- **Структура товара:**
  ```json
  {
    "id": 1,
    "name": "Название",
    "category": "Категория",
    "fabric_type": "slug",
    "price": 1290,
    "price_unit": "₽/м",
    "price_on_request": false,
    "fabric_meterage": 50,
    "image": "path/to/image.jpg",
    "colors": ["белый", "бежевый"],
    "is_new": true,
    "is_sale": false,
    "coming_soon": false
  }
  ```

### 6.2 Есть ли единый DataStore
- **ДА** — `CatalogDataStore` (`frontend/js/catalog/dataStore.js`)
- **Абстракция источников:**
  - `LocalJSONSource` — локальный JSON
  - `WordPressRESTSource` — WordPress REST API (подготовлен, не используется)
- **Методы:**
  - `load(source)` — загрузка данных
  - `getAllProducts()` — получение всех товаров
  - `getProductById(id)` — получение по ID
  - `detectParamType(key)` — определение типа параметра
  - `getUniqueValues(key)` — получение уникальных значений
  - `getFilterableKeys()` — получение фильтруемых ключей

### 6.3 Можно ли загрузить 1200+ товаров без переписывания логики
- **ДА** — технически возможно
- `CatalogDataStore` загружает весь JSON в память
- Фильтры работают с массивами в памяти (без серверных запросов)
- **ПОТЕНЦИАЛЬНЫЕ ПРОБЛЕМЫ:**
  - Загрузка 1200+ товаров: время загрузки может увеличиться
  - Рендеринг всех карточек на одной странице: производительность браузера
  - Пагинация уже реализована (`CatalogPagination`, `perPage: 8` в `catalog.html`)
  - Автоматическое определение фильтров может замедлиться на больших массивах

### 6.4 Отделены ли данные от UI и логики
- **ДА** — четкое разделение:
  - **Данные:** `CatalogDataStore` (абстракция источников)
  - **Логика фильтрации:** `CatalogFilters`
  - **Логика сортировки:** `CatalogSorting`
  - **Логика пагинации:** `CatalogPagination`
  - **UI рендеринг:** `CatalogProductCard`, `ProductPopup`
  - **Оркестрация:** `CatalogCore` (load → filter → sort → paginate)

---

## 7️⃣ ГОТОВНОСТЬ К ЧАСТЫМ ИЗМЕНЕНИЯМ

### 7.1 Можно ли добавлять/удалять/изменять товары БЕЗ правок в коде

**ДОБАВЛЕНИЕ/УДАЛЕНИЕ/ИЗМЕНЕНИЕ товаров:**
- **ДА** — достаточно изменить `products.json`
- Фильтры определяются автоматически из структуры данных
- Карточки строятся автоматически (без хардкода полей)

**НО есть захардкоженные части:**
1. **HTML фильтры в `catalog.html`:**
   - Тип ткани (кнопки со значениями)
   - Состав (чекбоксы со значениями)
   - Статус (чекбоксы со значениями)
   - **Если добавить новый тип ткани в JSON, его нужно вручную добавить в HTML**

2. **Валидация товаров:**
   - Обязательные поля: `['id', 'name', 'category', 'price', 'price_unit', 'image']`
   - Отсутствие любого из них → товар пропускается (с предупреждением в консоли)

### 7.2 Какие места критичны и могут поломаться при росте каталога

**КРИТИЧНЫЕ МЕСТА:**

1. **HTML фильтры (`catalog.html`):**
   - Захардкоженные кнопки типа ткани (строка 981-991)
   - Захардкоженные чекбоксы состава (строка 998-1021)
   - Захардкоженные чекбоксы статуса (строка 1061-1073)
   - **Риск:** новые значения в JSON не будут отображаться в фильтрах

2. **Автоматическое определение фильтров (`dataStore.js`, строка 85-143):**
   - `detectParamType()` работает только с типами: boolean, range, list (до 20 уникальных значений)
   - **Риск:** если уникальных значений > 20, фильтр не определится

3. **Пагинация (`catalog.html`, строка 1161):**
   - `perPage: 8` — временно уменьшено (в продакшене должно быть 24)
   - **Риск:** при росте каталога может потребоваться оптимизация

4. **Производительность фильтрации:**
   - Фильтры работают через `applyFiltersInternal()` с циклом по всем товарам
   - При 1200+ товарах может замедлиться (зависит от браузера)

**НЕ КРИТИЧНЫЕ (масштабируемые):**
- Структура данных отделена от логики
- Карточки товаров строятся автоматически
- Цвета, цены, плотность — динамические фильтры

---

## 8️⃣ ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

### 8.1 Архитектура модулей

**Модульная структура (`frontend/js/catalog/`):**
- `eventBus.js` — шина событий (единственный способ коммуникации)
- `dataStore.js` — хранилище данных (абстракция источников)
- `data-sources/localJSONSource.js` — локальный JSON источник
- `data-sources/wordpressRESTSource.js` — WordPress источник (не используется)
- `filters.js` — логика фильтрации
- `sorting.js` — логика сортировки
- `pagination.js` — логика пагинации
- `productCard.js` — рендер карточек в каталоге
- `catalog-core.js` — ядро (оркестрация: load → filter → sort → paginate)
- `integration.js` — документация интеграции

### 8.2 События (интеграция между модулями)

**Список событий:**
- `catalog:init` — инициализация каталога
- `catalog:loaded` — данные загружены
- `catalog:filtered` — фильтры/сортировка изменены
- `catalog:render` — рендер продуктов
- `catalog:openProduct` — открытие popup товара
- `product:addToCart` — добавление в корзину
- `cart:updated` — корзина обновлена

**Контракт корзины:**
```javascript
{
  productId: string,
  color: string | null,
  meters: number,
  rolls: number
}
```

### 8.3 Текущий размер каталога

**Количество товаров:** определяется из `products.json`  
**Формат:** JSON массив  
**Текущие данные:** ~363 строки JSON (несколько десятков товаров в примере)

---

## 📊 ИТОГОВАЯ ОЦЕНКА

### ✅ Сильные стороны:
- Модульная архитектура с четким разделением ответственности
- Единый DataStore с абстракцией источников
- Автоматическое определение фильтров из данных
- Поддержка динамических данных
- Событийная модель коммуникации

### ⚠️ Слабые места:
- Захардкоженные фильтры в HTML (`catalog.html`)
- Кнопка "Каталог товаров" не функциональна
- Поиск не реализован (только HTML)
- Отсутствует навигация по категориям
- Нет кнопок в карточке товара для перехода в каталог

### 🔧 Что требуется для масштабирования:
- Перенос захардкоженных фильтров в динамическую генерацию из данных
- Реализация поиска
- Реализация навигации по категориям
- Функционал кнопки "Каталог товаров"
- Оптимизация производительности для 1200+ товаров

---

**КОНЕЦ АНАЛИЗА**


