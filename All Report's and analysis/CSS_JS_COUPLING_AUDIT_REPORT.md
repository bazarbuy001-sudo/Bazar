# ОТЧЕТ: СВЯЗАННОСТЬ HTML/CSS И JAVASCRIPT

**Дата анализа:** 2024  
**Анализируемый проект:** frontend/  
**Методология:** Статический анализ кода (HTML, CSS, JavaScript)

---

## РЕЙТИНГ СВЯЗАННОСТИ: **6.5/10**

**Общая оценка:** Умеренная-высокая связанность. Визуальная часть и логика тесно переплетены в критических компонентах (header, каталог, попап). Многие компоненты используют прямые селекторы и манипуляции с классами, что создает риски при изменении HTML/CSS структуры.

---

## 1. СВЯЗАННОСТЬ СЕЛЕКТОРОВ

### Анализ использования селекторов в JavaScript

**Всего найдено уникальных селекторов:** ~50+

### Категории селекторов:

#### **1.1 ID селекторы (getElementById) - ВЫСОКИЙ РИСК**

**Критические ID, изменение которых сломает JS:**

| ID | Файл JS | Строка | Использование | Риск |
|---|---|---|---|---|
| `headerSticky` | index.html (inline) | 1740 | Инициализация header, добавление класса `scrolled` | ⚠️ КРИТИЧЕСКИЙ |
| `headerRow1` | index.html (inline) | 1741 | Добавление классов `collapsed`, `line-ready`, установка CSS переменных | ⚠️ КРИТИЧЕСКИЙ |
| `catalogGrid` | catalog.html (inline) | 1163 | Рендеринг товаров каталога | ⚠️ КРИТИЧЕСКИЙ |
| `sortSelect` | catalog.html (inline) | 1164 | Сортировка товаров | ⚠️ КРИТИЧЕСКИЙ |
| `filterColors` | catalog.html (inline) | 1253 | Динамическое добавление цветовых фильтров | ⚠️ КРИТИЧЕСКИЙ |
| `filterResetBtn` | catalog.html (inline) | 1474 | Сброс фильтров | ⚠️ КРИТИЧЕСКИЙ |
| `cartBadge` | index.html (inline) | 1904 | Обновление счетчика корзины | ⚠️ ВЫСОКИЙ |
| `videoCarousel` | index.html (inline) | 1617 | Карусель видео | ⚠️ ВЫСОКИЙ |
| `prevVideo` | index.html (inline) | 1655 | Навигация карусели | ⚠️ ВЫСОКИЙ |
| `nextVideo` | index.html (inline) | 1656 | Навигация карусели | ⚠️ ВЫСОКИЙ |
| `centerVideo` | stories.js | 12 | Центральное видео в stories | ⚠️ ВЫСОКИЙ |
| `leftPreviews` | stories.js | 13 | Левая панель preview | ⚠️ ВЫСОКИЙ |
| `rightPreviews` | stories.js | 14 | Правая панель preview | ⚠️ ВЫСОКИЙ |
| `productContainer` | product.js | 2 | Контейнер товара на странице | ⚠️ СРЕДНИЙ |

#### **1.2 Классовые селекторы (querySelector/querySelectorAll) - ВЫСОКИЙ РИСК**

**Критические классы для HEADER:**

| Класс | Файл | Использование | Риск |
|---|---|---|---|
| `.header-row-2` | index.html:1742 | Добавление класса `ready`, позиционирование | ⚠️ КРИТИЧЕСКИЙ |
| `.catalog-btn` | index.html:1743, catalogButton.js:36 | Кнопка каталога, открытие меню, добавление `is-active` | ⚠️ КРИТИЧЕСКИЙ |
| `.cart-btn` | index.html:1753 | Позиционирование, вычисление координат | ⚠️ КРИТИЧЕСКИЙ |
| `.row1-brand` | index.html:1744 | Добавление класса `positioned`, позиционирование | ⚠️ КРИТИЧЕСКИЙ |
| `.row1-nav` | index.html:1771 | Позиционирование навигации | ⚠️ КРИТИЧЕСКИЙ |
| `.header-sticky` | index.html:1803 | Добавление класса `scrolled` | ⚠️ КРИТИЧЕСКИЙ |

**Критические классы для КАТАЛОГА:**

| Класс | Файл | Использование | Риск |
|---|---|---|---|
| `.product-card` | catalog.js:275 | Добавление обработчиков клика, открытие попапа | ⚠️ КРИТИЧЕСКИЙ |
| `.mega-menu` | catalogButton.js:37, megaMenu.js:47 | Контейнер меню, добавление класса `is-open` | ⚠️ КРИТИЧЕСКИЙ |
| `.mega-menu__categories` | megaMenu.js:48 | Рендеринг категорий | ⚠️ ВЫСОКИЙ |
| `.mega-menu__subcategories` | megaMenu.js:49 | Рендеринг подкатегорий | ⚠️ ВЫСОКИЙ |
| `.mega-menu__category-item` | megaMenu.js:51 | Обработка hover/click на категории | ⚠️ ВЫСОКИЙ |
| `.mega-menu__subcategory-item` | megaMenu.js:52 | Обработка hover/click на подкатегории | ⚠️ ВЫСОКИЙ |
| `.video-card` | index.html:1684 | Позиционирование карусели | ⚠️ ВЫСОКИЙ |
| `.search-input` | searchController.js:63 | Поле поиска, обработка ввода | ⚠️ ВЫСОКИЙ |

**Критические классы для ПОПАПА:**

| Класс | Файл | Использование | Риск |
|---|---|---|---|
| `.product-popup` | product-popup.js:813 | Закрытие попапа по клику на overlay | ⚠️ КРИТИЧЕСКИЙ |
| `.product-popup__container` | product-popup.js:816 | Проверка клика внутри попапа | ⚠️ КРИТИЧЕСКИЙ |
| `.product-popup-overlay` | product-popup.js:813 | Закрытие попапа | ⚠️ ВЫСОКИЙ |
| `.product-popup__colors-section` | product-popup.js:1022 | Обновление секции цветов | ⚠️ ВЫСОКИЙ |

#### **1.3 Data-атрибуты - СРЕДНИЙ РИСК**

**Найдено использование data-атрибутов:**

| Data-атрибут | Файл | Использование | Риск |
|---|---|---|---|
| `data-product-id` | catalog.js | Получение ID товара при клике | ⚠️ СРЕДНИЙ |
| `data-category-id` | megaMenu.js:271 | ID категории в меню | ⚠️ СРЕДНИЙ |
| `data-subcategory-id` | megaMenu.js:308 | ID подкатегории в меню | ⚠️ СРЕДНИЙ |
| `data-fabric-type` | catalog.html:1747 | Фильтр по типу ткани | ⚠️ СРЕДНИЙ |
| `data-color` | catalog.html:1741 | Фильтр по цвету | ⚠️ СРЕДНИЙ |
| `data-action` | product-popup.js:832 | Обработка действий (close, selectColor и т.д.) | ⚠️ ВЫСОКИЙ |
| `data-currency-code` | product-popup.js:852 | Выбор валюты | ⚠️ СРЕДНИЙ |
| `data-index` | product-popup.js:863, 876 | Выбор цвета, изображения | ⚠️ СРЕДНИЙ |

---

## 2. ВЗАИМОДЕЙСТВИЕ JS С CSS

### 2.1 Манипуляции с классами через classList

**Найдено ~30+ использований classList.add/remove/toggle:**

#### **HEADER (index.html, inline JS):**

| Класс | Метод | Файл | Строка | Назначение | Риск |
|---|---|---|---|---|---|
| `scrolled` | add/remove | index.html | 1803, 1806 | Отслеживание скролла | ⚠️ КРИТИЧЕСКИЙ |
| `collapsed` | add/remove | index.html | 1804, 1807 | Скрытие верхней строки | ⚠️ КРИТИЧЕСКИЙ |
| `line-ready` | add | index.html | 1787 | Показ линии разделителя | ⚠️ КРИТИЧЕСКИЙ |
| `positioned` | add | index.html | 1790 | Показ бренда | ⚠️ КРИТИЧЕСКИЙ |
| `ready` | add | index.html | 1791 | Показ второй строки | ⚠️ КРИТИЧЕСКИЙ |

**CSS зависимости:**
- `.header-sticky.scrolled` - стили для липкой шапки
- `.header-row-1.collapsed` - скрытие строки (max-height: 0, opacity: 0)
- `.row1-brand.positioned` - показ бренда (opacity: 1, visibility: visible)
- `.header-row-1.line-ready::after` - показ линии разделителя

#### **MEGA MENU:**

| Класс | Метод | Файл | Строка | Назначение | Риск |
|---|---|---|---|---|---|
| `is-active` | add/remove | catalogButton.js | 58, 82 | Активное состояние кнопки | ⚠️ ВЫСОКИЙ |
| `is-open` | add/remove | catalogButton.js | 59, 83 | Открытое состояние меню | ⚠️ ВЫСОКИЙ |
| `no-scroll` | add/remove | catalogButton.js | 60, 84 | Блокировка скролла body | ⚠️ СРЕДНИЙ |
| `is-active` | add/remove | megaMenu.js | 397, 439 | Активная категория/подкатегория | ⚠️ ВЫСОКИЙ |

#### **SEARCH:**

| Класс | Метод | Файл | Строка | Назначение | Риск |
|---|---|---|---|---|---|
| `is-loading` | toggle | searchController.js | 364 | Состояние загрузки | ⚠️ СРЕДНИЙ |
| `is-active` | toggle | searchController.js | 365 | Активный поиск | ⚠️ СРЕДНИЙ |
| `has-results` | toggle | searchController.js | 366 | Наличие результатов | ⚠️ СРЕДНИЙ |
| `is-empty` | toggle | searchController.js | 367 | Пустой результат | ⚠️ СРЕДНИЙ |

#### **FILTERS (catalog.html):**

| Класс | Метод | Файл | Строка | Назначение | Риск |
|---|---|---|---|---|---|
| `active` | add/remove | catalog.html | 1289, 1742, 1749 | Активный фильтр (цвет, тип ткани) | ⚠️ ВЫСОКИЙ |

### 2.2 Изменение стилей напрямую через element.style

**КРИТИЧЕСКИЕ места:**

| Элемент | Свойство | Файл | Строка | Назначение | Риск |
|---|---|---|---|---|---|
| `headerRow1` | `--brand-center-left` | index.html | 1768 | CSS переменная для позиции бренда | ⚠️ КРИТИЧЕСКИЙ |
| `headerRow1` | `--nav-center-left` | index.html | 1777 | CSS переменная для позиции навигации | ⚠️ КРИТИЧЕСКИЙ |
| `headerRow1` | `--line-start` | index.html | 1785 | CSS переменная для начала линии | ⚠️ КРИТИЧЕСКИЙ |
| `headerRow1` | `--line-width` | index.html | 1786 | CSS переменная для ширины линии | ⚠️ КРИТИЧЕСКИЙ |

**Проблема:** JS напрямую устанавливает CSS переменные, которые используются в CSS для позиционирования. Изменение структуры HTML может сломать вычисления.

### 2.3 Зависимость анимаций/транзиций от JS

**Найдено:**

1. **Header transitions:**
   - CSS: `transition: max-height 0.3s ease, opacity 0.3s ease` (index.html:84-89)
   - JS управляет классами, которые триггерят эти transitions
   - **Риск:** Изменение CSS классов сломает анимации

2. **Mega Menu:**
   - CSS: `transform: translateX(-100%)` → `translateX(0)` (megaMenu.css:248, 254)
   - JS добавляет класс `is-open`, который меняет transform
   - **Риск:** Высокая связанность

3. **Product Popup:**
   - CSS transitions для открытия/закрытия
   - JS управляет классами открытия
   - **Риск:** Средний

---

## 3. СТРУКТУРНЫЕ ЗАВИСИМОСТИ

### 3.1 Поиск элементов по индексам

**Найдено использование навигации по DOM:**

| Навигация | Файл | Строка | Контекст | Риск |
|---|---|---|---|---|
| `parentElement` | searchController.js | 359 | Поиск контейнера input | ⚠️ СРЕДНИЙ |
| `querySelector` внутри элементов | product-popup.js | 1022 | Поиск `.product-popup__colors-section` внутри попапа | ⚠️ СРЕДНИЙ |
| `closest()` | product-popup.js | 816, 825 | Поиск родительских элементов | ✅ НИЗКИЙ (более устойчиво) |

**Вывод:** Структурные зависимости минимальны, используется в основном `querySelector` и `closest()`.

### 3.2 Зависимость от порядка элементов

**КРИТИЧЕСКИЕ места:**

1. **Header (index.html:1740-1791):**
   - JS вычисляет позиции между `.catalog-btn` и `.cart-btn`
   - **Зависимость:** Кнопки должны быть в определенном порядке в DOM
   - **Риск:** ⚠️ ВЫСОКИЙ - изменение порядка кнопок сломает позиционирование

2. **Stories (stories.js:26-50):**
   - Использует `innerHTML = ""` для очистки контейнеров
   - Добавляет элементы через `appendChild`
   - **Риск:** ⚠️ СРЕДНИЙ - зависит от структуры контейнеров

### 3.3 Вложенность элементов, важная для JS

**КРИТИЧЕСКИЕ вложенности:**

1. **Header структура:**
```
.site-header
  .nav-container
    #headerRow1
      .row1-brand
      .row1-nav
    #headerSticky
      .header-row-2
        .logo-icon
        .catalog-btn
        .search-box
        .cart-btn
```
**Риск:** ⚠️ ВЫСОКИЙ - JS использует getBoundingClientRect() для вычисления позиций между элементами

2. **Mega Menu структура:**
```
.mega-menu
  .mega-menu__categories
    .mega-menu__category-item
  .mega-menu__subcategories
    .mega-menu__subcategory-item
```
**Риск:** ⚠️ СРЕДНИЙ - JS рендерит структуру, но зависит от CSS классов

---

## 4. АДАПТИВНЫЕ КОМПОНЕНТЫ С JS

### 4.1 Header (Липкая шапка) - РИСК: ⚠️⚠️⚠️ КРИТИЧЕСКИЙ

**Связанность:** 9/10

**Зависимости:**
- ID: `headerSticky`, `headerRow1`
- Классы: `.header-row-2`, `.catalog-btn`, `.cart-btn`, `.row1-brand`, `.row1-nav`
- CSS переменные: `--brand-center-left`, `--nav-center-left`, `--line-start`, `--line-width`
- Классы для состояний: `scrolled`, `collapsed`, `line-ready`, `positioned`, `ready`

**Проблемы:**
- JS напрямую вычисляет позиции элементов через `getBoundingClientRect()`
- Устанавливает CSS переменные для позиционирования
- Добавляет классы, которые управляют видимостью и анимациями
- **Изменение HTML структуры header СЛОМАЕТ логику позиционирования**

### 4.2 Каталог товаров - РИСК: ⚠️⚠️ ВЫСОКИЙ

**Связанность:** 7/10

**Зависимости:**
- ID: `catalogGrid`, `sortSelect`, `filterColors`, `filterResetBtn`
- Классы: `.product-card`, `.catalog-grid`
- Data-атрибуты: `data-product-id`, `data-color`, `data-fabric-type`

**Проблемы:**
- JS рендерит товары в `catalogGrid`
- Использует класс `.product-card` для добавления обработчиков
- Фильтры зависят от ID контейнеров
- **Изменение ID контейнеров СЛОМАЕТ каталог**

### 4.3 Mega Menu - РИСК: ⚠️⚠️ ВЫСОКИЙ

**Связанность:** 7/10

**Зависимости:**
- Классы: `.mega-menu`, `.mega-menu__categories`, `.mega-menu__subcategories`, `.mega-menu__category-item`, `.mega-menu__subcategory-item`
- Классы состояний: `is-open`, `is-active`
- Data-атрибуты: `data-category-id`, `data-subcategory-id`

**Проблемы:**
- JS рендерит HTML структуру с определенными классами
- CSS зависит от этих классов для отображения
- **Изменение CSS классов СЛОМАЕТ меню**

### 4.4 Product Popup - РИСК: ⚠️ СРЕДНИЙ

**Связанность:** 6/10

**Зависимости:**
- Классы: `.product-popup`, `.product-popup__container`, `.product-popup-overlay`
- Data-атрибуты: `data-action`, `data-currency-code`, `data-index`
- Использует `closest()` для поиска родительских элементов (более устойчиво)

**Проблемы:**
- Зависит от структуры классов для закрытия по overlay
- Использует data-атрибуты для действий (хорошая практика)
- **Изменение классов контейнера СЛОМАЕТ логику закрытия**

### 4.5 Поиск (SearchController) - РИСК: ⚠️ СРЕДНИЙ

**Связанность:** 5/10

**Зависимости:**
- Класс: `.search-input`
- Классы состояний: `is-loading`, `is-active`, `has-results`, `is-empty`
- Селекторы: `.search-results`, `.search-clear`, `.search-form`

**Проблемы:**
- Зависит от класса `.search-input` для инициализации
- Использует классы состояний (хорошая практика)
- **Изменение класса input СЛОМАЕТ поиск**

### 4.6 Видео карусель (Stories) - РИСК: ⚠️ ВЫСОКИЙ

**Связанность:** 8/10

**Зависимости:**
- ID: `videoCarousel`, `prevVideo`, `nextVideo`, `centerVideo`, `leftPreviews`, `rightPreviews`
- Классы: `.video-card`, `.video-sound-toggle`
- Data-атрибуты: `data-position`

**Проблемы:**
- Жестко привязан к ID элементов
- Использует `innerHTML` для рендеринга
- **Изменение ID СЛОМАЕТ карусель**

---

## 5. ФОРМЫ И ИНПУТЫ

### 5.1 Валидация через JS

**Найдено:** Валидация не обнаружена в анализируемых файлах (возможно в других модулях).

### 5.2 Обработчики событий на полях

| Элемент | Событие | Файл | Строка | Риск |
|---|---|---|---|---|
| `.search-input` | `input`, `keydown` | searchController.js | 427, 428 | ⚠️ СРЕДНИЙ |
| `#sortSelect` | `change` (предположительно) | catalog.html | 1164 | ⚠️ ВЫСОКИЙ |
| Фильтры checkbox | `change` | catalog.html | 1460 | ⚠️ ВЫСОКИЙ |

### 5.3 Динамическое добавление/удаление полей

**Найдено:**
- Фильтры цветов динамически создаются в `#filterColors` (catalog.html:1253)
- **Риск:** ⚠️ СРЕДНИЙ - зависит от ID контейнера

### 5.4 Зависимость от атрибутов

**Data-атрибуты активно используются:**
- `data-action` - паттерн декларативных действий (✅ ХОРОШАЯ ПРАКТИКА)
- `data-currency-code`, `data-index` - передача данных
- `data-product-id`, `data-category-id` - идентификация

**Риск:** СРЕДНИЙ - data-атрибуты более устойчивы к изменениям CSS, но изменение названий сломает логику.

---

## КАРТА РИСКОВ

### 🔴 КОМПОНЕНТЫ С ВЫСОКОЙ СВЯЗАННОСТЬЮ (ОПАСНЫЕ ДЛЯ ИЗМЕНЕНИЙ)

1. **Header (липкая шапка)** - ⚠️⚠️⚠️ КРИТИЧЕСКИЙ
   - **Риск:** 9/10
   - **Почему:** JS напрямую вычисляет позиции элементов, устанавливает CSS переменные
   - **Что нельзя менять:**
     - ID: `headerSticky`, `headerRow1`
     - Классы: `.header-row-2`, `.catalog-btn`, `.cart-btn`, `.row1-brand`, `.row1-nav`
     - Порядок элементов в DOM
     - Структуру вложенности
   - **Что можно менять безопасно:**
     - Визуальные стили (цвета, размеры шрифтов, отступы)
     - Медиазапросы (если не влияют на позиционирование)

2. **Каталог товаров (catalog.html)** - ⚠️⚠️ ВЫСОКИЙ
   - **Риск:** 7/10
   - **Почему:** JS зависит от ID контейнеров, классов карточек
   - **Что нельзя менять:**
     - ID: `catalogGrid`, `sortSelect`, `filterColors`, `filterResetBtn`
     - Класс: `.product-card`
   - **Что можно менять безопасно:**
     - Стили карточек товаров
     - Layout фильтров (если сохранить ID)

3. **Mega Menu** - ⚠️⚠️ ВЫСОКИЙ
   - **Риск:** 7/10
   - **Почему:** JS рендерит HTML с определенными классами
   - **Что нельзя менять:**
     - Классы: `.mega-menu`, `.mega-menu__categories`, `.mega-menu__subcategories`
     - Классы состояний: `is-open`, `is-active`
   - **Что можно менять безопасно:**
     - Визуальные стили меню
     - Позиционирование (если не влияет на логику открытия)

4. **Видео карусель (Stories)** - ⚠️⚠️ ВЫСОКИЙ
   - **Риск:** 8/10
   - **Почему:** Жестко привязан к ID элементов
   - **Что нельзя менять:**
     - ID: `videoCarousel`, `prevVideo`, `nextVideo`, `centerVideo`, `leftPreviews`, `rightPreviews`
   - **Что можно менять безопасно:**
     - Визуальные стили карточек
     - Анимации

### 🟡 КОМПОНЕНТЫ С УМЕРЕННОЙ СВЯЗАННОСТЬЮ

5. **Product Popup** - ⚠️ СРЕДНИЙ
   - **Риск:** 6/10
   - **Почему:** Использует `closest()` (более устойчиво), но зависит от классов
   - **Что нельзя менять:**
     - Классы: `.product-popup`, `.product-popup__container`
     - Data-атрибуты: `data-action`
   - **Что можно менять безопасно:**
     - Внутренняя структура попапа (если сохранить основные классы)

6. **SearchController** - ⚠️ СРЕДНИЙ
   - **Риск:** 5/10
   - **Почему:** Зависит от класса input, но использует классы состояний (хорошая практика)
   - **Что нельзя менять:**
     - Класс: `.search-input`
   - **Что можно менять безопасно:**
     - Стили input
     - Классы состояний можно менять в JS и CSS синхронно

### 🟢 КОМПОНЕНТЫ С НИЗКОЙ СВЯЗАННОСТЬЮ (БЕЗОПАСНЫЕ ДЛЯ ИЗМЕНЕНИЙ)

7. **Catalog.js (legacy модуль)**
   - **Риск:** 4/10
   - **Почему:** Использует селекторы, но логика данных отделена
   - **Можно менять:** Визуальные стили карточек

8. **SEO Controller**
   - **Риск:** 2/10
   - **Почему:** Работает с `<head>`, не зависит от визуальных стилей
   - **Можно менять:** Все визуальные стили

---

## КРИТИЧЕСКИЕ СЕЛЕКТОРЫ (ПОЛНЫЙ СПИСОК)

### ID селекторы (КРИТИЧЕСКИЕ - изменение сломает JS):

```
#headerSticky          - Header логика (index.html:1740)
#headerRow1            - Header логика (index.html:1741)
#catalogGrid           - Каталог рендеринг (catalog.html:1163)
#sortSelect            - Сортировка (catalog.html:1164)
#filterColors          - Фильтр цветов (catalog.html:1253)
#filterResetBtn        - Сброс фильтров (catalog.html:1474)
#cartBadge             - Счетчик корзины (index.html:1904)
#videoCarousel         - Карусель видео (index.html:1617)
#prevVideo             - Навигация карусели (index.html:1655)
#nextVideo             - Навигация карусели (index.html:1656)
#centerVideo           - Stories (stories.js:12)
#leftPreviews          - Stories (stories.js:13)
#rightPreviews         - Stories (stories.js:14)
#productContainer      - Страница товара (product.js:2)
```

### Классовые селекторы (КРИТИЧЕСКИЕ):

**Header:**
```
.header-row-2          - Позиционирование, класс 'ready' (index.html:1742)
.catalog-btn           - Кнопка каталога, класс 'is-active' (index.html:1743, catalogButton.js:36)
.cart-btn              - Позиционирование (index.html:1753)
.row1-brand            - Позиционирование, класс 'positioned' (index.html:1744, 1790)
.row1-nav              - Позиционирование (index.html:1771)
.header-sticky         - Класс 'scrolled' (index.html:1803)
```

**Каталог:**
```
.product-card          - Обработчики клика (catalog.js:275)
.mega-menu             - Контейнер меню, класс 'is-open' (catalogButton.js:37, megaMenu.js:47)
.mega-menu__categories - Рендеринг категорий (megaMenu.js:48)
.mega-menu__subcategories - Рендеринг подкатегорий (megaMenu.js:49)
.mega-menu__category-item - Обработка hover/click (megaMenu.js:51)
.mega-menu__subcategory-item - Обработка hover/click (megaMenu.js:52)
```

**Поиск:**
```
.search-input          - Поле поиска (searchController.js:63)
```

**Попап:**
```
.product-popup         - Закрытие по overlay (product-popup.js:813)
.product-popup__container - Проверка клика внутри (product-popup.js:816)
.product-popup-overlay - Закрытие попапа (product-popup.js:813)
```

**Карусель:**
```
.video-card            - Позиционирование (index.html:1684)
.video-sound-toggle    - Переключение звука (index.html:1704)
```

### Классы состояний (управляются JS, используются в CSS):

**Header:**
```
.scrolled              - Липкая шапка (index.html:1803)
.collapsed             - Скрытие строки (index.html:1804)
.line-ready            - Показ линии (index.html:1787)
.positioned            - Показ бренда (index.html:1790)
.ready                 - Показ второй строки (index.html:1791)
```

**Mega Menu:**
```
.is-open               - Открытое меню (catalogButton.js:59)
.is-active             - Активная кнопка/категория (catalogButton.js:58, megaMenu.js:397)
.no-scroll             - Блокировка скролла body (catalogButton.js:60)
```

**Search:**
```
.is-loading            - Состояние загрузки (searchController.js:364)
.is-active             - Активный поиск (searchController.js:365)
.has-results           - Наличие результатов (searchController.js:366)
.is-empty              - Пустой результат (searchController.js:367)
```

**Filters:**
```
.active                - Активный фильтр (catalog.html:1289, 1742, 1749)
```

### Data-атрибуты (КРИТИЧЕСКИЕ):

```
data-product-id        - ID товара (catalog.js)
data-category-id       - ID категории (megaMenu.js:271)
data-subcategory-id    - ID подкатегории (megaMenu.js:308)
data-fabric-type       - Тип ткани (catalog.html:1747)
data-color             - Цвет (catalog.html:1741)
data-action            - Действие (product-popup.js:832) ⚠️ МНОЖЕСТВЕННОЕ ИСПОЛЬЗОВАНИЕ
data-currency-code     - Валюты (product-popup.js:852)
data-index             - Индекс (product-popup.js:863, 876)
data-position          - Позиция в карусели (index.html:772)
```

---

## РЕКОМЕНДАЦИИ ПО РЕФАКТОРИНГУ

### Приоритет 1: КРИТИЧЕСКИЕ (нужно переписать)

#### 1. Header (Липкая шапка)

**Проблема:** JS напрямую вычисляет позиции элементов и устанавливает CSS переменные.

**Рекомендации:**
- **Использовать CSS Grid/Flexbox** для позиционирования вместо вычисления координат
- **Вынести конфигурацию селекторов** в константы в начале файла
- **Использовать ResizeObserver** более аккуратно (уже частично используется)
- **Минимизировать использование CSS переменных** для позиционирования
- **Паттерн:** Использовать CSS классы для состояний вместо вычисления координат

**Пример рефакторинга:**
```javascript
// Вместо:
headerRow1.style.setProperty('--brand-center-left', catalogBtnLeft + 'px');

// Использовать:
// CSS: .header-row-1.has-catalog-btn .row1-brand { left: var(--catalog-btn-left); }
// JS: headerRow1.classList.add('has-catalog-btn');
```

#### 2. Каталог товаров

**Проблема:** Зависимость от ID контейнеров.

**Рекомендации:**
- **Использовать data-атрибуты** для идентификации контейнеров вместо ID
- **Вынести селекторы в конфигурацию** модуля
- **Использовать делегирование событий** вместо привязки к каждому `.product-card`

**Пример:**
```javascript
// Вместо:
const catalogGrid = document.getElementById('catalogGrid');

// Использовать:
const SELECTORS = { grid: '[data-catalog-grid]' };
const catalogGrid = document.querySelector(SELECTORS.grid);
```

#### 3. Видео карусель (Stories)

**Проблема:** Жесткая привязка к ID.

**Рекомендации:**
- **Использовать data-атрибуты** или классы вместо ID
- **Вынести конфигурацию** в объект
- **Создать класс StoriesCarousel** вместо глобальных функций

### Приоритет 2: ВАЖНЫЕ (улучшить)

#### 4. Mega Menu

**Рекомендации:**
- Уже использует хорошую практику с константами SELECTORS и CLASSES ✅
- Можно улучшить: использовать data-атрибуты вместо классов для структуры
- **Паттерн:** BEM-классы для структуры, data-атрибуты для логики

#### 5. Product Popup

**Рекомендации:**
- Уже использует `closest()` и data-атрибуты ✅
- Можно улучшить: вынести селекторы в константы
- **Паттерн:** Продолжать использовать data-атрибуты для действий

### Приоритет 3: ЖЕЛАТЕЛЬНЫЕ (оптимизация)

#### 6. SearchController

**Рекомендации:**
- Уже использует константы SELECTORS и CLASSES ✅
- Можно улучшить: сделать классы состояний настраиваемыми

#### 7. Общие рекомендации

**Паттерны для уменьшения связанности:**

1. **Константы селекторов:**
   ```javascript
   const SELECTORS = {
     header: '[data-header]',
     button: '[data-catalog-button]'
   };
   ```

2. **Data-атрибуты вместо классов для логики:**
   ```html
   <!-- Вместо: -->
   <div class="product-card" data-product-id="123">
   
   <!-- Использовать: -->
   <div class="product-card" data-product-id="123" data-action="open-product">
   ```

3. **CSS классы только для состояний:**
   - Структурные классы: `.product-card`, `.header-row-2`
   - Классы состояний: `.is-open`, `.is-active`, `.scrolled`
   - Логика через data-атрибуты: `data-action`, `data-product-id`

4. **Делегирование событий:**
   ```javascript
   // Вместо:
   cards.forEach(card => card.addEventListener('click', handler));
   
   // Использовать:
   container.addEventListener('click', (e) => {
     const card = e.target.closest('.product-card');
     if (card) handler(e);
   });
   ```

5. **Использование CSS Grid/Flexbox вместо вычисления координат:**
   - Убрать `getBoundingClientRect()` для позиционирования
   - Использовать CSS для layout

---

## ИТОГОВАЯ ОЦЕНКА И ВЫВОДЫ

### Общая связанность: **6.5/10**

**Оценка по компонентам:**

| Компонент | Связанность | Риск изменений |
|---|---|---|
| Header | 9/10 | ⚠️⚠️⚠️ КРИТИЧЕСКИЙ |
| Каталог | 7/10 | ⚠️⚠️ ВЫСОКИЙ |
| Mega Menu | 7/10 | ⚠️⚠️ ВЫСОКИЙ |
| Видео карусель | 8/10 | ⚠️⚠️ ВЫСОКИЙ |
| Product Popup | 6/10 | ⚠️ СРЕДНИЙ |
| Search | 5/10 | ⚠️ СРЕДНИЙ |
| Catalog.js (legacy) | 4/10 | 🟢 НИЗКИЙ |
| SEO Controller | 2/10 | 🟢 НИЗКИЙ |

### Выводы:

1. **Header - самая критическая точка** связанности. Изменение HTML структуры header сломает логику позиционирования.

2. **Каталог и Mega Menu** имеют высокую связанность, но используют более структурированный подход с константами.

3. **Product Popup и Search** используют хорошие практики (data-атрибуты, closest()), связанность умеренная.

4. **Видео карусель** имеет высокую связанность из-за жесткой привязки к ID.

### Рекомендации по порядку рефакторинга:

1. **Сначала:** Header (критическая точка)
2. **Затем:** Видео карусель, Каталог, Mega Menu
3. **Потом:** Product Popup, Search (улучшение практик)
4. **В конце:** Оптимизация остальных модулей

### Безопасные изменения (можно делать сейчас):

- Визуальные стили (цвета, шрифты, отступы)
- Медиазапросы (если не влияют на позиционирование)
- Анимации и transitions (если не меняют классы состояний)
- Стили компонентов с низкой связанностью (SEO, Catalog.js legacy)

### Опасные изменения (требуют осторожности):

- Изменение ID элементов (особенно header, каталог, карусель)
- Изменение классов, используемых в JS (особенно header, mega menu)
- Изменение структуры DOM header
- Изменение порядка элементов в header
- Изменение data-атрибутов, используемых в JS

---

**Конец отчета**

