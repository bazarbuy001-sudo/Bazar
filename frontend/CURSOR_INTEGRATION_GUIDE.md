# CURSOR_INTEGRATION_GUIDE.md
## Инструкция по внедрению модулей каталога BazarBuy

**Версия:** 1.0.0  
**Статус:** ОБЯЗАТЕЛЬНО К ИСПОЛНЕНИЮ  
**Роль Cursor:** ТОЛЬКО внедрение, НЕ проектирование

---

## 📁 СТРУКТУРА ФАЙЛОВ

### Новые файлы (созданные Claude)

```
frontend/
├── js/
│   ├── menu/
│   │   ├── catalogButton.js      ← Кнопка «Каталог товаров»
│   │   └── megaMenu.js           ← Mega Menu (навигация)
│   ├── search/
│   │   └── searchController.js   ← Поиск
│   ├── seo/
│   │   └── seoController.js      ← SEO (meta/canonical/noindex)
│   └── catalog/
│       ├── paginationFix.js      ← Исправление пагинации
│       └── dataStoreNavigation.js ← Расширение DataStore
├── css/
│   └── megaMenu.css              ← Стили для меню
```

### Существующие файлы (НЕ ТРОГАТЬ структуру)

```
frontend/
├── js/
│   ├── catalog/
│   │   ├── dataStore.js          ← CatalogDataStore (СУЩЕСТВУЕТ)
│   │   ├── eventBus.js           ← CatalogEventBus (СУЩЕСТВУЕТ)
│   │   ├── filters.js            ← CatalogFilters (СУЩЕСТВУЕТ)
│   │   ├── sorting.js            ← CatalogSorting (СУЩЕСТВУЕТ)
│   │   ├── pagination.js         ← CatalogPagination (СУЩЕСТВУЕТ)
│   │   ├── catalog-core.js       ← CatalogCore (СУЩЕСТВУЕТ)
│   │   └── productCard.js        ← Карточка товара (СУЩЕСТВУЕТ)
│   ├── product-popup.js          ← ProductPopup (СУЩЕСТВУЕТ)
│   └── integration.js            ← Документация событий (СУЩЕСТВУЕТ)
```

---

## 🔌 ПОРЯДОК ПОДКЛЮЧЕНИЯ СКРИПТОВ

### В index.html (главная страница)

Добавить **ПОСЛЕ** существующих скриптов каталога, **ПЕРЕД** закрывающим `</body>`:

```html
<!-- ============================================ -->
<!-- НОВЫЕ МОДУЛИ КАТАЛОГА (добавить после существующих) -->
<!-- ============================================ -->

<!-- 1. Расширение DataStore (ОБЯЗАТЕЛЬНО ПЕРВЫМ) -->
<script src="js/catalog/dataStoreNavigation.js"></script>

<!-- 2. Исправление пагинации -->
<script src="js/catalog/paginationFix.js"></script>

<!-- 3. Меню -->
<script src="js/menu/megaMenu.js"></script>
<script src="js/menu/catalogButton.js"></script>

<!-- 4. Поиск -->
<script src="js/search/searchController.js"></script>

<!-- 5. SEO -->
<script src="js/seo/seoController.js"></script>

<!-- 6. Инициализация новых модулей -->
<script>
document.addEventListener('DOMContentLoaded', function() {
    // Инициализация кнопки каталога
    if (window.CatalogButton) {
        CatalogButton.init();
    }
    
    // Инициализация Mega Menu
    if (window.MegaMenu) {
        MegaMenu.init('.mega-menu');
    }
    
    // Инициализация поиска
    if (window.SearchController) {
        SearchController.init({
            inputSelector: '.search-input',
            debounceDelay: 300,
            minQueryLength: 2
        });
    }
    
    // Инициализация SEO
    if (window.SEOController) {
        SEOController.init({
            siteName: 'BazarBuy'
        });
    }
});
</script>
```

### В catalog.html (страница каталога)

Аналогично, но SEO инициализируется автоматически на странице каталога.

### Подключение CSS

В `<head>` добавить:

```html
<link rel="stylesheet" href="css/megaMenu.css">
```

---

## 🏗️ НЕОБХОДИМЫЕ HTML-ЭЛЕМЕНТЫ

### Кнопка «Каталог товаров»

Должна существовать (уже есть по аудиту):

```html
<button class="catalog-btn">
    <span>☰</span>
    <span>Каталог товаров</span>
</button>
```

### Контейнер Mega Menu

Добавить рядом с кнопкой (внутри того же родителя):

```html
<div class="mega-menu">
    <!-- Содержимое генерируется автоматически -->
</div>
```

### Поле поиска

Должно существовать (уже есть по аудиту):

```html
<input type="text" class="search-input" placeholder="Поиск товаров...">
```

Опционально — кнопка очистки:

```html
<button class="search-clear" type="button">×</button>
```

---

## 📡 СОБЫТИЯ (EventBus)

### Существующие события (из integration.js)

| Событие | Назначение | Эмитит | Слушает |
|---------|------------|--------|---------|
| `catalog:init` | Инициализация каталога | CatalogCore | — |
| `catalog:loaded` | Данные загружены | CatalogCore | MegaMenu, SearchController |
| `catalog:filtered` | Фильтры/сортировка изменены | CatalogCore | SEOController |
| `catalog:render` | Рендер продуктов | CatalogCore | — |
| `catalog:openProduct` | Открытие popup товара | CatalogProductCard | ProductPopup |
| `product:addToCart` | Добавление в корзину | ProductPopup | CartStore |
| `cart:updated` | Корзина обновлена | CartStore | ProductPopup |

### Новые события (добавленные модулями)

| Событие | Назначение | Эмитит | Слушает |
|---------|------------|--------|---------|
| `menu:open` | Меню открыто | CatalogButton | MegaMenu |
| `menu:close` | Меню закрыто | CatalogButton | MegaMenu |
| `search:change` | Поиск изменён | SearchController | SEOController |

---

## ⚠️ КРИТИЧЕСКИЕ ЗАМЕЧАНИЯ

### 1. Метод getNavigation() в DataStore

Модуль `dataStoreNavigation.js` добавляет метод `getNavigation()`.

**Если данные навигации приходят из WordPress:**

В `WordPressRESTSource` нужно добавить:

```javascript
// В wordpressRESTSource.js
getNavigation: function() {
    // Возвращает данные из WP REST API
    // Формат: массив объектов с полями:
    // category_id, category_name, subcategory_id, 
    // subcategory_name, fabric_type_id, fabric_type_name, order
    return this._navigationCache || [];
}
```

**Если используется LocalJSONSource:**

Добавить файл `data/navigation.json`:

```json
[
    {
        "category_id": "knitwear",
        "category_name": "Трикотаж",
        "subcategory_id": "knit_under",
        "subcategory_name": "Бельевой",
        "fabric_type_id": "kulirka_knit",
        "fabric_type_name": "Кулирка",
        "order": 10
    }
]
```

### 2. Пагинация

Модуль `paginationFix.js` автоматически патчит `CatalogPagination`.

**Проверить после внедрения:**
- Страница 1 показывает товары 1-24
- Страница 2 показывает товары 25-48
- НЕ все товары до текущей страницы

### 3. SEO на страницах с фильтрами

При активных фильтрах/поиске SEO автоматически:
- Устанавливает `noindex,follow`
- Canonical указывает на чистый URL

---

## ✅ ЧЕКЛИСТ ВНЕДРЕНИЯ

### Файлы

- [ ] Скопирован `js/menu/catalogButton.js`
- [ ] Скопирован `js/menu/megaMenu.js`
- [ ] Скопирован `js/search/searchController.js`
- [ ] Скопирован `js/seo/seoController.js`
- [ ] Скопирован `js/catalog/paginationFix.js`
- [ ] Скопирован `js/catalog/dataStoreNavigation.js`
- [ ] Скопирован `css/megaMenu.css`

### HTML

- [ ] Добавлен контейнер `.mega-menu`
- [ ] Подключены скрипты в правильном порядке
- [ ] Подключен CSS

### Проверка работы

- [ ] Кнопка «Каталог товаров» открывает/закрывает меню
- [ ] ESC закрывает меню
- [ ] Клик вне меню закрывает его
- [ ] Категории отображаются в меню
- [ ] Hover раскрывает подкатегории
- [ ] Поиск работает с debounce 300ms
- [ ] Пустые категории скрыты
- [ ] Пагинация показывает только текущую страницу
- [ ] При фильтрах/поиске появляется `noindex`

---

## 🚫 ЗАПРЕЩЕНО

1. **НЕ менять** существующие файлы DataStore, EventBus, Filters
2. **НЕ добавлять** новые хранилища или state-менеджеры
3. **НЕ хардкодить** категории, подкатегории, типы ткани
4. **НЕ изменять** структуру событий
5. **НЕ подключать** React, Vue, jQuery или другие библиотеки
6. **НЕ обращаться** напрямую к WordPress API (только через DataStore)

---

## 📋 ПОСЛЕ ВНЕДРЕНИЯ

Сообщить о результатах:

1. Все файлы скопированы и подключены
2. Модули инициализируются без ошибок в консоли
3. Кнопка каталога работает
4. Меню строится из данных
5. Поиск фильтрует каталог
6. Пагинация исправлена

---

## 🔧 ОТЛАДКА

### Консольные сообщения

При успешной инициализации:
```
[CatalogButton] Инициализирован
[MegaMenu] Инициализирован
[SearchController] Инициализирован
[SEOController] Инициализирован
[PaginationFix] Метод applyPagination исправлен
[DataStoreNavigation] Метод getNavigation добавлен
```

### Частые ошибки

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `CatalogDataStore не найден` | Неправильный порядок скриптов | Подключить модули ПОСЛЕ dataStore.js |
| `Элемент кнопки не найден` | Нет `.catalog-btn` в HTML | Добавить кнопку |
| `Контейнер не найден` | Нет `.mega-menu` в HTML | Добавить контейнер |
| `Навигация пустая` | Нет данных в DataStore | Проверить загрузку данных |

---

**Конец инструкции**

